# 07 — 安全与已知取舍

## 敏感数据数据流(SSH 密码 / 私钥口令 / SMTP 密码,已审计闭环无泄漏)

```
前端密码框(type=password)
  → invoke('encrypt_password', {plain})      # SSH 密码唯一一次明文过 IPC
  → Rust: windows-dpapi encrypt(CurrentUser) → base64
  → servers.json 的 auth.password_enc(密文落盘)
运行期:
  前端始终传 password_plain: null(或临时输入)
  → 后端 resolve_password:dpapi_unprotect(password_enc) → 内存瞬时明文 → SSH 认证

私钥口令(阶段三,key_pass_enc):服务器表单输入(不回显)
  → 「记住口令」勾选:连接成功后 DPAPI 加密存 auth.key_pass_enc;不勾=仅本次连接使用,不落盘
  → 运行期 resolve_key_passphrase:dpapi_unprotect(key_pass_enc) → SshClient::connect 透传
    load_secret_key(key_path, passphrase);口令错误 → 中文映射「私钥口令错误或私钥已损坏」

SMTP 密码(阶段二,notify.json 的 email.password_enc):通知中心表单输入
  → notify_save_config:非空明文 → dpapi_protect 落盘;留空 → 保留已存密文
  → 查询脱敏:notify_get_config 只回 passwordSaved 布尔值,明文/密文都不出后端
  → 发送时 resolve_email_password 解密(内存瞬时);发信经 lettre(内存拼接,不落日志)

不变量:
  - 不存在解密命令;dpapi_unprotect 仅在 resolve_password / resolve_key_passphrase /
    config_io(导出解密→导入重加密)/ notify(SMTP 密码)内部调用
  - 明文/密文不进日志、不进事件、不进 localStorage、不进错误信息
  - DPAPI CurrentUser 作用域:同机同用户才可解;直接拷贝 config/ 到其他机器后密码必须重录
    (跨机器迁移用「配置中心」导出/导入:blob 内为口令加密的明文,导入时本机重新 DPAPI 加密)
```

## 配置导出加密格式(阶段三,config_io.rs)

```
文件 = JSON 信封 {"version":1, "kdf":"argon2id", "salt_b64", "nonce_b64", "data_b64"}
密钥  = Argon2id(导出口令, salt 16B 随机;Params::default: m=19MiB, t=2, p=1)→ 32 字节
data  = AES-256-GCM(认证加密,nonce 12B 随机)的明文 JSON:
        {"servers":[...], "projects":[...], "notify":{...}}
        blob 内 SSH 密码/私钥口令/SMTP 密码为明文(可移植),靠信封加密保护
导入口令错误 / 文件被篡改 → GCM 标签校验失败,统一报「导出口令错误或文件已损坏」(二者不可区分,有意为之)
导出口令不入文件;丢失口令 = 数据不可恢复(无后门)
```

## 注入防护

| 面 | 防护 |
|---|---|
| 本机 Docker CLI | `Command::new("docker").args([...])` 参数数组,不经 shell;镜像名/tag 不拼字符串 |
| SSH 远端命令 | 全部经单引号转义(部署管线 `shell_single_quote`,远程管理 manage 层 `shell_quote`——实现相同:单引号包裹 + `'\''` 转义)后才拼进命令;远端 compose 路径/`remote_dir`/服务名/路径/容器名/卷名/网络名均覆盖;本地 Windows compose 路径先上传并替换为远端路径;cleanup 的 glob 刻意留在引号外(否则不展开),引号内前缀已转义——有防注入单测;shell 探测脚本不含单引号(有单测保证转义无损);阶段一二新增:manifest.json 与 `.env` 内容经 base64(`A-Za-z0-9+/=` 无 shell 元字符)写入,路径仍加引号;`.env` 原子写命令的 `$$`(PID)刻意拼在引号外,展开为纯数字无注入面(有单测锁定命令形态) |
| 文件映射路径 | `remote_join`:rel 以 `/` 开头不逃逸(仍相对 remote_dir 拼接);**不防 `..` 相对逃逸**(见下方取舍);本地路径不存在时报错含路径 |
| compose 解析 | serde_yaml 结构化解析,不 eval;重复键 YAML 直接解析失败(安全侧) |
| 前端 DOM | 全部 `createElement`/`textContent`,禁 innerHTML 拼数据(镜像名/服务器名按不可信输入处理;notify/config-io/settings 三个新模态同样纪律) |
| 外链打开 | `open_external` 仅允许 `https://` 前缀(当前唯一来源是 GitHub Release 页,防御性校验) |
| 子进程窗口 | Windows 下统一 `CREATE_NO_WINDOW`(GUI 进程防闪黑框) |
| 清理执行(第三批) | 待删目标**由后端自己从扫描结果拼命令**:镜像 ID/容器 ID/卷名经 `shell_single_quote`;归档目录路径由 `project_dir_of_release`/`remote_join` 从服务器扫描输出推导(不接受前端传任意路径);前端回传的 `releaseDirs`/`imageRefs` 仍逐个单引号包裹后再拼(有防注入单测) |
| 回滚目标目录(第三批) | `rollback_execute_stack_at` 要求 `dir` 为**绝对路径**(非 `/` 开头直接 Err);发布目录由 `dir + releases/<ts>` 拼接后**校验路径前缀**,越出项目目录即中止;compose 路径与 `-f` 链一律单引号包裹 |
| 源 compose 更新(第三批) | 复制目标固定为项目配置内的 `compose_file` 所在目录,文件名固定 `docker-compose.yml`/`.env`/override 同名;源路径来自 `ProjectConfig`(由用户导入时写入),不从前端任意传参 |

## 主机密钥:TOFU 校验(阶段三实现,原「无条件接受」安全债已清)

`ssh.rs` 的 `ClientHandler::check_server_key` 按 per-server 指纹校验(UPGRADE-PLAN 风险声明修改点 1):

- **指纹口径**:OpenSSH 风格 `SHA256:` + base64(nopad)(SHA-256(公钥 SSH wire blob)),与 `ssh-keygen -lf` 一致(russh 的 `PublicKey::fingerprint` 补前缀)
- **首次连接**(配置 `host_key_sha256` 为 None):接受并把观察到的指纹交由命令层 `persist_host_key_if_needed` 落盘(TOFU 信任-on-first-use;连接失败路径不落盘)
- **已有记录且匹配** → 接受
- **不匹配** → 拒绝连接,固定文案:「服务器主机密钥已变更!可能为服务器重装/换 IP,也可能存在中间人风险。如确认无误,请在服务器管理中重新信任该主机。」
- **重置**:03 服务器管理每服务器展示当前指纹(等宽)+「重新信任」按钮 → `retrust_host_key` 清空记录(二次确认),下次连接重新 TOFU
- 部署主线与远程管理全部建连路径(commands.rs 与 manage.rs 的 `connect_server`)统一接入;指纹记录随配置中心导出/导入迁移

## 设计决策记录(裁决史,改相关代码前先读)

| # | 决策 | 理由 | 代价 |
|---|---|---|---|
| 1 | 配置存应用目录(便携),`DD_CONFIG_DIR` 语义=应用目录(内部拼 `config/`) | 免安装污染;测试注入 | 装进 Program Files 等只读目录会配置失败(错误清晰) |
| 2 | Docker 走 CLI 子进程,不直连 Engine API | Windows named pipe 处理简单;CLI 语义稳定 | 每次调用 spawn 进程(check_host 一次 4 个;有 CREATE_NO_WINDOW) |
| 3 | gzip 用 flate2 库流式,不依赖系统 gzip | Windows 无自带 gzip | 无 |
| 4 | size_bytes 按 1024 进制换算 | 简单一致 | docker 实际 SI 千进制,显示有少量偏差 |
| 5 | DPAPI 依赖 windows-dpapi 0.1(小版本 crate) | 源码核实 Scope::User 正确 | 不宜盲目升级(0.2 签名变) |
| 6 | russh 0.46 + russh-sftp 2.4(计划书写 0.45) | 0.45 缺所需 API | 锁定版本 |
| 7 | 服务器管理页不受宿主环境闸门锁定 | 配置编辑不依赖 Docker;仅部署动作受闸门 | 无 |
| 8 | 仅 image 无 build 的服务默认 Pull(即使本地 Exact 匹配) | 公共镜像本地通常也有,误判面更小;分类表一键可切 | 手动 build 的用户需点一次切换 |
| 9 | Missing+has_build(写了 image 未 build)不加警告 | 属正常工作流("还没 build");✗ 徽章已提示;部署期 save 报错兜底 | 极少量噪音换不误报 |
| 10 | serde_yaml 0.9 + 显式 `apply_merge()` | 0.9 不自动应用 `<<` 合并键(曾致锚点 compose 误解析,已修+fixture) | 0.9 已 archived,关注社区续作 |
| 11 | compose 副本上传为远端 `docker-compose.yml` | `-f` 需要远端文件;本地副本路径在服务器无效 | 覆盖远端同名文件(项目目录语义内) |
| 12 | releases 留档保留 5 份,时间戳到秒 | 免手动回滚基础 | 同秒并发混包(单用户可接受);磁盘占用 |
| 13 | 日志文件 target 全构建启用;Stdout 仅 debug | release GUI 进程无 stdout,fern 写入会 panic(复审抓出的回归) | release 排障只能看文件 |
| 14 | 全局单取消标志;前端部署中禁重复发起 | 简单;单窗口场景够用 | 后端层并发 deploy 会互相冲掉取消(前端已防) |
| 15 | 单镜像勾选日期标签时,服务器 load 后追加 `docker tag <日期tag> <原引用>` | 让 compose 引用的原 tag 同步指向新镜像,变更检测生效(否则容器静默不更新);日期 tag 保留作版本档案 | 服务器上历史日期 tag 随部署次数累积(可 `docker image prune` 外的按需清理;悬空旧层由 prune 处理) |
| 16 | 健康检查 `compose ps --all` + ExitCode 语义(exited≠0→失败;=0→继续观察;字段缺失→保守判失败) | 默认 ps 只列 running,"启动即退出"的服务会被健康服务掩盖而误判通过 | exit-0 一次性初始化服务会误报,报错文案已提示关闭健康检查 |
| 17 | 镜像包上传失败后同路径重试一次(2s 退避) | 激活 SFTP 断点续传(否则每次部署新时间戳/uuid,续传永远不可达) | 非瞬态失败也会多试一次(≤2s+1 次) |
| 18 | webhook URL 日志脱敏(只记 host;ureq 错误只记状态码/Kind) | webhook URL 常内嵌 token,全量落日志有凭据泄露面 | 诊断信息略少(Transport source 被舍弃) |
| 19 | 默认镜像名兜底扫描:候选=顶层 name: → origin.json 原始目录名 → 父目录名;docker.io 豁免私有仓库提示 | build 无 image 是高频场景;origin.json 解决副本路径目录名失真;docker.io 显式前缀误报登录提示属噪音 | exit-0/非常规命名(container_name、build.tags)不在覆盖范围 |
| 20 | 全表单失败走强化通道(formFailLoud:内联+滚动 60ms 延迟+toast),缺项聚合提示 | 错误框在长表单顶部而视口在底部 = "点了没反应";点击按钮的原生焦点滚动会覆盖同步滚动,故延迟一拍 | 无 |
| 21 | 项目配置新增 health_wait_secs / pre_deploy_cmd / post_deploy_cmd / notify_webhook(全 serde default) | 生产底线:备份钩子、部署结果如实上报 | 旧配置缺字段自动兼容 |
| 22 | 远程管理独立成 manage* 四模块,不并入 commands.rs | 部署主线与远程管理正交(依赖方向:manage→ssh/commands::resolve_password,commands 不反向依赖 manage);独立新文件支持并行开发 | 连接助手/密码解析需 pub(crate)/pub |
| 23 | 实时监控用「轮询 docker stats --no-stream + 事件流」而非长驻流式通道 | 语义简单可靠;一条 SSH 连接跨轮复用(exec 每轮各自开新通道)让真实帧间隔贴近设定值 | 帧间隔 ≥ 服务端采样耗时(慢主机上 >设定间隔) |
| 24 | 监控全局单会话(generation 代号)+ 终端单会话防护;重复 start 自动替换旧会话 | 单用户单窗口,同时只看一台服务器;旧会话自清理免泄漏 | 不能同时监控多服务器(前端切换时统一清理) |
| 25 | stats 单轮失败四分类(Success/PermDenied/CommandFailed/TransportFailed),仅 stopped=true 才让前端停止 | 命令失败不该拆连接,传输失败必须重连;权限拒绝无重试意义 | 传输判定依赖错误文案子串(遗留 P2,见已知限制 13) |
| 26 | 交互终端为行式简易终端(剥 ANSI + \r 行首覆盖 + 命令历史),不引入 xterm.js | 保持零 npm 运行时依赖/无打包器;后端仅纯追加 exec_interactive | 复杂全屏 TUI(top/vim)渲染不完美(见已知限制 14) |
| 27 | 终端 shell 启动前自动探测(存在性+可执行性验证),前端默认「自动(推荐)」 | 真机取证:`command -v` 发现不了"文件在但不可执行"(退出码 126),探测期拦截优于打开即结束 | 探测多一次非交互 docker exec(毫秒级) |
| 28 | 智能传输判定 = 镜像 ID(内容寻址,`docker image inspect .Id` / `docker images --no-trunc` 完整 64 位口径),无时间戳、无状态文件 | `docker load` 后远端 `.Id` 与本地严格相等 → ID 相等即内容一致;零维护成本 | 远端必须 `--no-trunc`(12 位截断 ID 与完整 ID 永不相等,回归单测锁定);远端取不到 ID 保守回退全量 |
| 29 | 回滚留档 = 发布目录 manifest.json + compose 副本;DeployRecord 增 release_dir(serde default) | manifest 记录逐服务 tag/包名/跳过标记,compose 副本让回滚可复现当时编排;旧记录/旧发布优雅降级 | manifest/副本写入尽力而为,可能缺失(回滚降级沿用现有 compose) |
| 30 | 整栈回滚前做**留档归属校验**(manifest.project ≠ 当前项目即中止) | 多项目共用 remote_dir 时防误回滚对方项目留档;在任何 docker load 之前读,中止时远端状态未动 | 手工改过项目名的历史发布需核对 |
| 31 | 通知中心独立 notify.json(serde default;NOTIFY_UNHEALTHY 损坏期间跳过写回) | 通知是旁路功能,损坏不应拖垮 load_config;回退默认值写回会不可逆地丢 DPAPI 密文 | 损坏期间保存其他配置不落 notify;需显式重存或重启 |
| 32 | 邮件用 lettre 0.11(rustls ring provider)而非调系统邮件客户端 | 纯 Rust 无 openssl 原生依赖;ssl/starttls/none 三模式可控 | 错误分类依赖 lettre Error kind(已按源码核实六类映射) |
| 33 | 主机密钥 per-server 指纹存 `ServerConfig.host_key_sha256`,不建全局 known_hosts 文件 | 与现有 per-server 配置模型一致;导出/导入随配置迁移;实现面最小 | 无全局中央记录(单机单用户场景不需要) |
| 34 | 配置导出 = Argon2id+AES-256-GCM 口令加密信封,blob 内敏感字段为明文;导入整体替换不合并 | DPAPI 密文绑定机器不可迁移,必须「解密→重加密」;GCM 提供防篡改;整体替换语义简单可预期 | 依赖用户口令强度;导入覆盖当前全部配置(强确认提示) |
| 35 | 检查更新自实现(GitHub releases/latest + reqwest),不用 tauri-plugin-updater;取版本两段式——主路径请求 releases/latest 页读 302 `Location` 头解析 tag(免 API 配额),api.github.com 调用降为回退 | 后者需生成/保管签名密钥并托管 latest.json,单发行渠道收益低;未来要静默更新再升级(记录在案);api.github.com 未认证 60 次/小时按出口 IP 计,共享代理出口实测极易 403,而 html 页 302 重定向无配额 | 无自动下载安装,「前往下载」跳 Release 页;回退路径 notes 注明「经 API 回退」 |
| 36 | 托盘关闭拦截每次关闭事件**现读** settings.json(不缓存) | 设置保存即生效,无重启/同步成本 | 每次关闭一次文件读(微秒级) |
| 37 | AppSettings 用 camelCase serde 对齐前端;损坏回退默认值无 UNHEALTHY 保护 | 设置项(closeToTray/proxy)无不可再生敏感数据,回退无损 | 与其余配置 snake_case 口径不同(例外清单见 04) |
| 38 | `.env` 经 base64 传输 + 256KB 上限 + 临时文件 PID 后缀原子写 | base64 防 JSON 引号/换行/编码损坏;PID 后缀防并发交错;mv 同文件系统 rename 原子 | 非 UTF-8 文件 lossy 读入(U+FFFD 警示);>256KB 拒绝在应用内编辑 |
| 39 | 「一键清除」范围 = 4 个 json + 保留 logs;前端 DELETE 输入强确认 | 日志对排障有价值;强确认防误触不可逆操作 | 清除不含 stacks/ 副本目录与 resume-deploy.json(导入的项目副本仍在,projects.json 已删即不可达,可手动清理) |
| 40 | 部署断点 = 独立 resume-deploy.json 键值表(键=服务器\|项目\|模式,上限 10 条裁最旧;损坏按空表自愈),每步边界落盘;批量部署不落断点 | 断点是尽力而为的旁路数据,不应让部署失败有额外副作用或让部署页不可用;上限防无限膨胀 | 断点期本地临时 tar 保留占临时盘(放弃/成功即清);artifacts 结构随 mode 不同、由 commands 层私有约定 |
| 41 | 批量部署逐台**串行**(不并行)、复用单发管线内层(仅替换 server_id);批量路径不发 deploy-progress/deploy-done、不落断点 | 串行避免多台并发争抢本机 Docker/临时盘与全局单取消标志;复用内层保证语义与单发严格一致;批量单台失败整台重跑(断点粒度与批量编排冲突,取舍从简) | 多台部署总时长为各台之和;批量中途失败不能从「该台的步骤 N」续传 |
| 42 | 清理分析 = 只读预览(cleanup_preview)+ 勾选节定向 prune(cleanup_execute),不提供 docker system prune 一键全清 | 预览让用户先看清明细(哪些悬空镜像/停止容器/未用卷)再决定;逐节执行单节失败不影响其余;Docker prune 天然只清未使用资源 | 仍依赖用户理解 prune 语义(卷清理不可恢复,前端有二次确认);新增未使用卷在容器删除后才会出现,预览是时点快照 |
| 43 | 批量部署事件 `deploy-batch` 为 camelCase(例外清单成员),deploy-log 批量期加 `[服务器名] ` 前缀 | 与第二批升级新增契约结构统一 camelCase 纪律;前缀让共享日志区可区分多台输出 | deploy-log 语义在批量期与非批量期略有差异(前缀),前端按需剥离展示 |
| 44 | 实时跟随用 `logs -f` 流式 exec(select 取消通道)+ 全局单流;关闭开关/弹窗即主动 close 通道 | `logs -f` 语义最直白(与一次性日志同一命令族);主动 close 保证「关闭即停流」不等远端;单流与监控/终端会话模型一致 | 后端 streamId 前端未知,事件过滤按「当前活跃会话唯一」(单流语义下无歧义);行数上限 5000 由前端裁剪 |
| 45 | 镜像迁移走「源 save\|gzip 原始通道直写本地 → sftp 上传目标 → load」,不经任何镜像仓库;目标同 ID 自动跳过 | 与部署「方案 A」离线传输同一哲学;原始通道(Data 块直写)保证二进制安全且**源端零落盘**;同 ID 跳过复用智能传输的 `same_image_id` 口径 | 逐镜像串行无断点(与批量部署同取舍);大镜像占用本地临时盘(守卫随用随删) |
| 46 | 清理分析:无标签镜像改为 `docker images` 全量拉取后**客户端过滤** `<none>`,弃用 `docker images -f dangling=true` | 该过滤在 BuildKit / containerd image store 下常返回空,而 `docker images` 明确列出 `<none>:<none>` —— 表现为「有悬空镜像却识别不到」;客户端过滤口径由本应用掌控,不依赖 docker 版本行为 | 需要多传一份完整镜像列表(通常几十行);在用项靠容器引用集合判定,故要多一次 `docker inspect` |
| 47 | 清理执行改为**显式目标列表逐条删除**(rmi ID / rm ID / volume rm / rm -rf),弃用 `prune -f` | prune 的删除范围由 docker 自行判定,与预览勾选项可能不一致(`volume prune` 会连未列出的未使用卷一起删);逐条删除让「所见即所删」,执行结果与预览一一对应 | 条目多时命令行较长(仍受 SSH exec 单命令长度限制);依赖前端回传的 ID/路径,后端不做二次扫描 |
| 48 | 分项目清理的扫描起点可配(默认服务器 `remote_dir`,可填 `/home`),深度上限 4 层并排除 `releases/` 与 `.git` | 用户目录结构不统一(服务器可能是 `/home/<user>/<项目>` 多级);上限与排除项避免把归档里的 compose 副本当成独立项目、避免遍历整盘 | 目录更深时需手动把起点指到项目父目录;`releases/` 之外的归档命名(如自定义目录)不会被识别 |
| 49 | 独立回滚中心的项目清单 = 目录扫描 + docker compose labels 合并;**归属校验从「项目名匹配」改为「路径前缀」** | 服务器上的目录名未必等于应用内项目名(甚至不是本应用部署的),按目录为中心才贴合实际;路径前缀(归档必须位于所选项目目录下)仍能防止跨项目误回滚 | 单镜像回滚(切标签)仍需软件内 projectId,故对未配置项目只提供整栈回滚 |
| 50 | 项目「从源更新」按 compose+.env+override 的**内容哈希**判定,启动时自动执行(可关) | 哈希覆盖插值相关的全部输入,只有真正影响部署的内容变化才触发更新;自动执行免去「改了 compose 还得手动重新导入」 | 哈希不含文件 mtime(改回原内容不算变更);`unknown`(旧配置无哈希)一律不动,需用户手动更新一次才进入比对 |
| 51 | 项目「从源更新」保留 `service_overrides`(仅在源中仍存在的服务沿用旧分类),其余字段不动 | 服务分类是用户在应用内的劳动成果,不应被源文件覆盖;映射/钩子/健康检查等与 compose 内容无关 | 源中消失的服务其分类丢失(重建同名服务需重新分类);旧副本备份为 `.bak` 供人工对照 |
| 52 | 自动更新安装以 **`/S /R`** 启动 NSIS(GUI 不参与),靠 `config/update-pending.json` 标记告诉新版「刚更新过」 | Tauri 模板只在静默/被动模式下解析 `/R` 并重启应用;静默安装是无人值守场景的必然选择。标记放 `config/`(安装器不动该目录)保证跨更新存活 | 安装器必须能读到 `/R`(依赖 Tauri 模板行为,已核对生成物);标记与实际版本不一致时不提示(安装中途失败不误报) |
| 53 | 「导入项目」未绑定源时显示「绑定源」入口(`bind_project_source`),而非只对已绑定者显示按钮 | v5.4.0 之前导入的项目没有 `source_compose_path`,按旧条件它们永远看不到按钮 —— 功能形同未实现;以 `imported`(副本是否在 `config/stacks/` 下)判定资格,才能覆盖存量项目 | 绑定需用户选一次文件(一次性);手工项目(远端相对路径)不提示绑定,避免噪音 |
| 55 | 部署目录从服务器级下沉到**项目级**(`ProjectConfig.remote_dir`,留空回落 `ServerConfig.remote_dir`) | 服务器只有一个 `remote_dir`,同服务器多项目共用同一部署目录与 `docker-compose.yml`,切换项目必须改服务器配置;下沉后各项目互不干扰。回落语义保证**旧配置行为完全不变** | 填了独立目录的项目需在服务器上先建好该目录(表单提示);同一服务器的多项目若都留空则仍共用目录(向后兼容的代价) |
| 56 | 项目「默认服务器」仅作**便利**(部署页自动带出),后端不校验归属 | 临时把某项目部署到另一台服务器是合理用法(换机迁移、灰度验证),强制校验会挡掉正当操作 | 选错服务器不会被拦截,需用户自己确认(部署页提示行会显示实际部署目录与来源) |
| 57 | 部署历史补 `server_id`/`project_id`(serde default),`resolveRecordIds` 优先 id、回退按名 | 旧实现只存名称,服务器或项目**改名后回滚按钮即失配**(按名反查失败);存 id 后改名不再影响回滚 | 旧记录仍无 id,只能按名回退;两边都对不上时给明确提示(不静默失败) |
| 58 | 归档保留数量下沉为**项目配置**(`release_keep`,0-50,`None` = 默认 5),由 `release_keep_of` 解析(超上限夹到 50) | 写死 5 个无法适配不同项目的留档需求(重要项目想多留、实验项目想少占空间);回落语义保证旧配置行为不变 | 填 0 等于不留历史归档(无法回滚到上一版),表单已写明语义;上限 50 防手改配置写入过大值 |
| 59 | 回滚中心逐版本删除的两个新命令都做**独立校验**(归档:路径前缀必须落在 `<dir>/releases/` 之下;镜像:`inspect` 先校验存在再 `rmi` 且**不带 `-f`**) | 删除不可逆,不能只靠前端传参可信:路径前缀防 `ts` 注入逃逸,`rmi` 不带 `-f` 保证仍被容器引用的镜像删不掉(与清理分析同口径)—— docker 自身会拒绝并给出原因 | 镜像被引用时删除失败(用户需先停用该容器),这是刻意的安全取舍而非缺陷 |
| 54 | 更新结果的 `changed`/`unchanged` 按「**更新前**副本 vs 源」的内容比较判定(专用 `bundle_content_changed`,只比内容不比文件名) | 旧实现先写新哈希再复用 `project_source_status` 复核 —— 基准刚被本次写入覆盖,结果**恒为 unchanged**:文件确实同步了却提示「无改动」,用户无法判断是否生效。另:`source_content_hash` 把文件名纳入哈希,而副本恒名 `docker-compose.yml`,源若叫 `compose.yml` 内容相同也会假阳性,故判定须走纯内容比较 | 判定多一次内容读取(几 KB 级);`changed` 文案给出服务数,`unchanged` 明确"源与副本一致" |

## 已知限制 / 合并后待办(按优先级)

1. ~~host key TOFU(安全债首位)~~ **已实现**(阶段三,per-server 指纹 + 变更拒绝 + 重新信任;见上文专节)
2. 部署向导未强制重跑宿主机检测(依赖 check 页状态 + localStorage 恢复的短暂解锁窗口;后端失败安全)
3. `image_filter` 配置字段暂未被消费(项目下拉不过滤镜像)
4. BusyBox/Alpine 服务器 `df -PBG` 拿不到磁盘数据(预检告警跳过;主流 Debian/Ubuntu 无碍)
5. SFTP 上传中取消不生效(步骤边界生效);~~断点续传~~ **已实现**(v0.4.0 失败后同路径重试触发;阶段六升级为**部署断点续传**——每步边界落盘 checkpoint,失败/取消后从步骤 N 续跑)
6. compose 的 `${VAR}` 插值仅支持同目录 `.env`(不支持 `--env-file` 多文件/shell 环境)
7. import_compose 写配置失败时残留空 `stacks/<uuid>/` 目录(无功能影响)
8. 后端日志的 `log::warn!` 在 release 只落文件不外显(设计如此)
9. 部署历史:并发部署结束时理论丢一条;panic 路径不记录(单用户桌面场景可接受)
10. 部署预览按 remote_dir 基名过滤容器:compose 顶层 `name:` / 路径含大写等规范化场景会 miss(保守判 Create,不影响部署正确性)
11. 健康检查对 exit-0 一次性初始化服务会误报(报错文案已提示关闭健康检查)
12. 表格行 hover 反白时 badge-info 文字不可见(系统既有样式债,建议加 hover 豁免)
13. 远程管理·监控:传输层失败判定依赖中文错误文案子串(`is_transport_error` 匹配固定短语;C 阶段审查遗留 P2,结构化错误码为后续改进)
14. 远程管理·终端:简易 ANSI 剥除对 OSC/ST 与部分 CSI 边界覆盖不全(行式终端场景够用;复杂 TUI 程序如 top/vim 显示不完美;阶段五的 resize 只同步行列数,不改变渲染模型)
15. ~~`manage_exec_resize` 前端未接线~~ **已接线**(阶段五:打开/模态尺寸变化/窗口缩放时字符量测上报,失败可重试,eof 后停发)
16. 远程管理·栈扫描仅覆盖 remote_dir 深度 ≤2 的 4 种标准 compose 文件名(自定义文件名/更深目录不识别)
17. `.env` 编辑:非 UTF-8 文件(如 GBK)经 lossy 读入显示 U+FFFD——**直接保存会把替换符写回服务器造成乱码**(前端有警示条,靠用户确认);`.env` 超 256KB 拒绝在应用内编辑(提示直接上服务器改)
18. 检查更新:主路径(releases/latest 302 重定向)**无 API 配额**;仅回退路径 api.github.com 受未认证 60 次/小时限额(按出口 IP 计),403 限流时明确提示稍后重试;代理仅作用于检查更新请求(不影响部署/SSH)
19. 托盘 tooltip 固定文案「DockerDeploy SSH」,未随部署状态动态变化(计划中的增强,当前为静态)
20. 通知中心桌面通知依赖 Windows 系统通知组件(勿扰模式/专注助手开启时可能不展示,发送侧不感知)
21. 批量部署与断点续传互斥:批量不落断点、不支持逐台续传,单台失败整台重跑(决策 #41);批量 deploy-log 带 `[服务器名] ` 前缀,与其他时期格式略异(决策 #43)
22. ~~帮助系统「03 服务器管理」章节的「清理优化」条目仍描述旧版内联确认条~~ **已同步**(第三批:改写为分析模态 + 新增「06 回滚中心」章节 + 从源更新与映射默认名说明)
23. 停止容器的清理分析口径含 `status=created`(从未启动的容器)与 `status=exited`;`docker container prune -f` 同口径清理,无 `--filter until` 时间窗(全量清理勾选项)
24. 批量部署取消时,当前台内 SFTP 大文件传输不即时中断(协作式取消,跑完当前步骤边界才止),余台立即跳过
25. 日志实时跟随:后端 streamId 为后端代号而前端未知,事件过滤按「当前活跃会话唯一」——若未来放开多流,需改为流代号精确过滤;单行超 16KB 截断(超长堆栈行显示省略提示)
26. 镜像迁移:取消在镜像边界生效,单镜像大文件传输中不即时中断;同名不同 ID 的镜像会被覆盖(旧镜像变悬空,日志告警);本地中转占用临时盘(迁移完成后守卫即删)
27. 清理分析的分项目归属:**日期标签镜像按 compose 内 `image:` 仓库名匹配**——compose 中未写 image(纯 build)或镜像名经变量插值后与服务器实际仓库名不符时,该项目的标签不会出现在分项目块中(不影响四节通用清理)
28. 清理分析的分项目扫描:`find` 深度上限 4 层(起点之下),且排除 `releases/` 与 `.git`;归档目录名须匹配 `20*-*`(标准时间戳)——自定义命名归档不会被列出
29. 清理执行的入参由**前端回传显式目标**,后端不做二次校验扫描:若预览与执行之间服务器状态变化(如镜像在别处被删/被容器引用),对应条目会在输出中报错但**其余条目照常执行**(逐节 catch,不中断)
30. 项目「从源更新」的 `unknown` 状态(**旧配置无 `source_hash`** 或手工项目)不参与启动自动更新,需在项目卡片手动点一次「从源更新」写入哈希后才进入自动比对
31. 独立回滚中心的项目列表里,`runningContainers` 以容器名包含目录名近似归属(无 compose labels 时);`docker ps` 不可用或标签缺失时该计数可能为 0(不影响回滚操作本身)
32. 独立回滚中心的单镜像回滚依赖软件内已配置项目(`rollback_execute_single` 需 projectId 与 compose 配置):服务器上存在但未在软件内配置的项目只提供整栈回滚(前端明确提示)
33. ~~项目「从源更新」按钮不可见~~ **已修复**(v5.4.x):旧条件要求 `source_compose_path` 非空,而 v5.4.0 之前导入的项目该字段为空 → 改为按 `imported` 显示按钮并补「绑定源」入口;更新结果原本只有 toast 一闪而过,现增设**源检查汇总栏**(状态计数 + 待办项目 + 最近操作结果)常驻项目表上方
33b. ~~手动「从源更新」总提示「无改动」~~ **已修复**(v5.4.2):`update_project_from_source` 先写新哈希再复用 `project_source_status` 复核,基准被本次写入覆盖 → 结果恒为 unchanged(文件其实已同步)。改为用更新前的副本与源做**纯内容**比较(`bundle_content_changed`,忽略 compose 文件名差异 —— 副本恒名 `docker-compose.yml`,源叫 `compose.yml` 时按旧哈希会假阳性)。前端文案相应区分:「已从源同步改动到配置副本(N 处服务)」vs「源与配置副本一致,无需更新」
34. ~~自动更新装完不重启、无提示~~ **已修复**(v5.4.x):`update_install` 只传 `/S`(Tauri 模板仅在静默/被动模式解析 `/R`)→ 改为 `/S /R` 自动重启;并写 `update-pending.json` 标记供新版提示「已更新到 vX」。**注意**:修复只对本版(v5.4.x 及以后)构建的安装包生效,旧版本下载新包时仍会装完不重启(需手动打开一次)
35. 自动更新的「已更新到 vX」提示依赖标记与实际版本一致:若安装中途失败(仍是旧版),标记被静默丢弃不提示(避免误报),但用户也不会看到失败说明 —— 需自行确认版本号
36. 更新标记路径可经环境变量 `DD_UPDATE_PENDING_PATH` 覆盖(单测隔离用);生产环境该变量不应设置
37. 项目级部署目录为新配置项:已部署过旧目录的项目**不会自动迁移** —— 填了独立目录后需在服务器上建好该目录,且原目录中的 compose/releases 留档不会自动搬运(可手动迁移后切换,或继续留空沿用服务器目录)
38. 同一台服务器上多个项目若都未填项目级目录,仍共用服务器的 `remote_dir` 与同一个 `docker-compose.yml`(向后兼容所致):这种配置下部署会互相覆盖 compose 文件,建议为每个项目分配独立目录
39. 部署页项目下拉的「本机」排序与自动带出服务器依赖 `default_server_id`:未指定默认服务器的项目不参与排序与自动切换(仍可选,行为同旧版)
40. 部署历史里的旧记录(无 `server_id`/`project_id`)在服务器或项目改名后仍无法回滚,只能给「配置可能已变更」提示;重新部署一次即可写入带 id 的新记录
41. ~~「检查源变更」点击后无任何提示~~ **已修复**(第四批补丁):手动点击现在**总是**给出可见反馈 —— toast 摘要 + 汇总栏常驻结果;此前只有结果栏渲染,而"没有导入项目"时整块隐藏、也不弹提示,点完看起来毫无反应(边界文案:「没有可检查的项目」/「没有可检查的导入项目(N 个均为手工项目)」)
42. ~~检查更新点「前往下载」闪黑框~~ **已修复**(第四批补丁):`open_external` 经 `cmd /c start` 拉起浏览器时缺 `CREATE_NO_WINDOW`,`update_install` 有而这里漏了;现已补上(两处生产 spawn 均有该标志)
43. 归档保留数量填 **0** 表示部署后清空全部历史归档(仅留本次发布):**将无法回滚到任何历史版本**,清理分析面板也会把该项目的全部旧归档列为可清理项(表单与面板文案均已写明)
44. 回滚中心的镜像「删除」不带 `-f`:仍被任何容器(含已停止)引用时由 docker 拒绝并回传原因 —— 需先删除/停用相关容器;这是与「清理分析跳过在用镜像」一致的安全口径
45. 回滚中心删除归档只删该归档目录(`rm -rf <dir>/releases/<ts>`),**不影响**当前运行的服务与 `docker-compose.yml`(与「回滚到此归档」不同,后者会恢复 compose 副本并 up)

## 测试现状

- `cargo test`:**239 passed** 全绿(解析/匹配/插值/路径拼装/命令拼装/防注入/配置往返/DPAPI roundtrip/镜像 ID 口径/manifest 组装/回滚文案/信封 seal-open/版本比较/重定向 Location 解析/通知校验与错误分类/端口兜底/设置 camelCase 序列化/断点裁剪与往返/批量请求校验/清理 NDJSON 容错与命令拼装/日期标签与 compose 仓库名提取/du 解析/路径 basename/源哈希与状态判定)
- `#[ignore]` 真机测试 **12 个**(docker/ssh 实连,含 `image_id_by_ref` 与检查更新重定向主路径真机比对、阶段九 `exec_streaming` 自然结束+yes 流取消、阶段十 `sftp_download` 0x00-0xFF 全字节 roundtrip):`cargo test -- --ignored`
- 前端:node --check + DOM 桩冒烟(实现期);无自动化前端测试框架(约定保持轻量)
- 审查史:每模块经独立代码审查 + 修复轮;UPGRADE-PLAN 各批均走「实施 → 审查 → 验收」流程后合入(第一批随 v5.1.0、阶段六至八随 v5.2.0、阶段九/十随 v5.3.0、第三批阶段十一至十四随 v5.4.0 发布),记录见 `UPGRADE-PLAN.md` 完成记录
