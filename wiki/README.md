# DockerDeploy SSH — 项目 Code Wiki

> 一套文档读完全项目。新会话从这里开始。

## 这个项目是什么

**Windows 桌面客户端(Tauri 2),把"本地构建的 Docker 镜像部署到自己的生产服务器"做成一键操作。**

背景:不使用第三方镜像仓库(Docker Hub/ACR 等)、不搭 CI/CD,部署走"方案 A"离线传输:

```
本地 docker save → gzip 压缩 → SFTP 上传 → 服务器 docker load → docker compose up -d
```

支持两种部署模式:

| 模式 | 单位 | 流程 | 适用 |
|---|---|---|---|
| **单镜像部署** | 一个镜像 | 打日期 tag → save+gzip → 上传 → 文件同步 → load → compose up(5 步) | 单服务项目 |
| **整栈部署(compose)** | 整个 compose 栈 | 导入 compose → 服务分类(本地传输/服务器拉取)→ 多镜像打包上传 → load → compose pull/up(6 步) | compose 编排的项目 |

配套能力:宿主机/服务器环境检测闸门(未通过禁止部署,提供一键启动/安装)、多服务器管理(SSH 密钥/密码/**加密私钥口令**三认证,密码 DPAPI 加密存储,主机密钥 TOFU 校验)、部署项目配置持久化、**智能传输(跳过未变化镜像)**、**部署断点续传**(失败/取消后从步骤 N 继续)、**多服务器批量部署**(逐台串行,独立历史可逐台回滚)、服务器端 releases 留档**一键回滚**、**通知中心**(桌面+SMTP 邮件)、**配置中心**(口令加密导出/导入/一键清除)、**设置中心**(系统托盘/检查更新/主题跟随系统)、**清理分析**(悬空镜像/停止容器/未用卷/build cache 预览+定向 prune)、**远程管理面板**(05 页:容器/镜像/卷/网络/Compose 栈/.env/实时监控/PTY 交互终端/**日志实时跟随**/**跨服务器镜像迁移**/**项目跨服务器迁移**含数据卷)、全站帮助系统(13 章节)。

## 文档目录(按阅读顺序)

| 文档 | 内容 | 何时读 |
|---|---|---|
| [01-架构总览.md](01-架构总览.md) | 技术栈、系统分层、三条数据流、目录结构 | 第一篇,建立全局图景 |
| [02-后端模块.md](02-后端模块.md) | 14 个功能 Rust 模块职责、关键结构体/函数签名、超时常量 | 改后端前 |
| [03-前端说明.md](03-前端说明.md) | 页面结构、JS 模块(含 manage/notify/config-io/settings/help)、全局约定(AppState/AppBus)、设计语言 | 改前端前 |
| [04-契约参考.md](04-契约参考.md) | **106 个命令 + 12 个事件 + 配置 JSON Schema 完整速查** | 跨前后端改动的对账表 |
| [05-构建与运行.md](05-构建与运行.md) | 环境要求、dev/build 命令、安装布局、日志、配置文件夹 | 跑起来之前 |
| [06-部署流程与回滚.md](06-部署流程与回滚.md) | 两条部署管线的逐步语义、智能传输、一键回滚、独立回滚中心(06 页)、环境检测闸门规则 | 理解/调试部署行为 |
| [07-安全与已知取舍.md](07-安全与已知取舍.md) | 敏感数据数据流、导出加密格式、注入防护、设计决策记录、已知限制 | 评估改动影响时 |

## 30 秒速览

```
仓库:D:\Github-repositories\docker-deploy-ssh(git,主分支 main)
代码量:Rust ~29,000 行(33 文件,含 main.rs;commands/ 为 11 文件子模块目录)+ 原生 JS/HTML/CSS ~22,600 行(无框架无打包器,字体 woff2 自捆绑)
技术:Tauri 2 + tokio + russh/russh-sftp + flate2 + serde_yaml + windows-dpapi + tauri-plugin-dialog
      + tauri-plugin-notification + lettre(rustls)+ argon2 + aes-gcm + reqwest(rustls+socks)+ ureq
前端调用后端:window.__TAURI__.core.invoke(106 个命令),事件 12 个,字段默认 snake_case
      (notify/config-io/settings/update/回滚列表与回滚中心/.env/批量/断点/清理/项目源更新/日志流/迁移/项目迁移契约为 camelCase,例外清单见 04)
页面:6 页导航(01 检测 / 02 镜像 / 03 服务器 / 04 部署 / 05 远程管理 / 06 回滚中心)+ 设置中心 + 全站帮助
构建:npm run tauri dev / npm run tauri build(产物 NSIS 安装包 ~8.2MB)
配置:安装目录 config/ 下 servers.json + projects.json + notify.json + settings.json +
      deployments.json + resume-deploy.json(便携式;原子写);日志 logs/app.log
测试:cargo test(纯函数单测 534 passed;真机测试 #[ignore] 15 个)
当前版本:v6.16.0(第三十四批(二)+(三),同一未发版版本合并 —— ①(P3)up 前**服务端权威解析校验** —— 部署/回滚在 up 前跑 `docker compose config --format json`(compose ≥ v2.2 提供;可部署环境经 `--pull never` 已要求 ≥ v2.15),把服务端权威解析的 `services.*.image` 与记录逐服务比对(两侧同口径规范化:补 `:latest`、剥 `docker.io` 前缀):**部署侧不一致即阻断**(附逐服务明细与常见原因——服务器 shell 环境变量 / `.env` 漂移 / compose 副本不一致;compose 语法错误也在此提前暴露),**回滚侧只告警**(插值漂移预检已是「确认制」,再阻断会与刚做出的确认冲突,紧急回滚优先,实际结果由 up 后校验兜底);老版 compose 报 unknown flag 时告警跳过。同批 D:**up 后校验的不一致数并入部署结果文案**(「部署完成;up 后校验:N 个服务…不一致(详见日志)」),与回滚链同口径,历史/通知不再只写「部署完成」。测试 519→**528**(+9;含本机 compose v5.4.0 实测抓样的 golden 单测),命令 122 不变。③(L2)服务器体检增强:环境检测新增 `docker system df` 可回收空间摘要(镜像 / 容器 / 卷 / 构建缓存;磁盘紧张时部署预检提示「可回收 ≈ X」与容器日志占用)与容器日志体积(非 root 读不到则缺省)—— 均为**可选字段**,探测失败不进 `errors`、不参与判定;④(S3)迁移「目标链」真机 `#[ignore]` 样板:源/目标目录三件套经 SFTP 搬运(归档字节级一致)→ 服务端权威解析(复用 P3 命令)→ 目标 `up` 并自证容器在运行。测试 528→**531**(+3 纯函数单测),真机样板 14→**15**。⑤(池外·第三十三批留档)容器内**多选批量传输**:文件管理列表加勾选列(表头全选/半选)+ 批量条(批量下载:文件与目录、选一次本机目录串行下载;取消即中止并标记剩余项),工具栏「批量上传」可一次选多个本地文件串行上传(复用覆盖前备份语义),结束渲染逐条结果;纯前端零新命令。⑥(池外·第三十三批留档)宿主机**白名单读写**:设置中心「通用」区新增「宿主机可写目录」(绝对路径前缀,默认空 = 维持只读),文件管理「部署目录」源仅白名单内可写 —— 判定在服务端(`readlink -f` canonicalize 后按目录边界匹配,软链不能逃逸),覆盖前备份与二次确认沿用;顺带补齐 DeployDir 的写入/备份分支(`unreachable!()` 与「静默跳过备份」两个真缺口)。测试 531→**534**(+3)。⑦(池外·第三十三批留档)层级增量传输**评估**(L1):受控实测(本机 Docker,同构两版仅改应用一行)整包 3.65 MB vs 裁剪包 30 KB,且「只删服务器已有层 blob 后 `docker load`」实测成功且可运行 → 轻量增量路线可行;**结论:值得做但建议单独立批**(归档一致性/格式兼容/分片续传为主要风险),是否立批待拍板;实验后本机 Docker 已归零。此前版本:v6.15.0(第三十四批(一):静态守护 + 文档清尾 —— 新增 `verify/static-integrity.js`(**词法级未声明自由标识符**,补 scope-integrity 的运行时盲区;DOM id 三查:静态唯一 / 引用可解析 / 动态重名评审制,已入 CI);**首跑抓出并修复两处真 bug** —— ①批量续传登记 `serverId: sid`(第二十八批 B1 复合键重构漏删声明的回归:失败/取消台登记抛 ReferenceError,后续队列停摆且续传入口不登记),②设置入口取 `settings-btn`(实际 `settings-entry-btn`,dock 版本徽点「点击打开设置中心」静默失效);顺带管理页 .env 确认按钮 id 命名空间化(`env-confirm-*`,消除跨文件重名)+ wiki/06、07 TOCTOU 措辞更新(watchtower 已归档 → CI / 运维脚本)。测试 519 不变、命令 122 不变。此前版本:v6.14.0(第三十三批:远程管理新增「**文件管理**」—— 容器 rootfs / 数据卷 / 部署目录(只读)三源,浏览 / 上传 / 下载 / 文本编辑 / 新建目录 / 改名 / 删除,覆盖前默认把原文件备份到本机 `config/fm-backups/`(同文件保留 3 份);容器源走 `docker cp` 通道(镜像里没有任何工具、容器已停止都能双向传),数据卷经临时容器挂载,部署目录只读;含**容器快照**(实际环境变量默认掩码 / 端口 / 进程 / 挂载,并可与 compose 副本的 `environment:` 键名双向对比)与**同栈文件分发**(串行推送到同 compose 项目的其它容器);命令 114→**122**,测试 510→**519**。此前版本:v6.13.0(**第三十一批 + 第三十二批审查修复**,同一未发版版本合并 —— 第三十一批:部署/回滚流程加固 —— ①**P1 孤儿容器**:部署/回滚/迁移/栈启停的 `up`(与栈 `down`)统一带 `--remove-orphans`,同项目内不在当前 compose 的容器一并移除(此前回滚到旧归档或删掉服务后,孤儿容器继续运行而界面报「完成」;栈停止时孤儿还占着网络使 down 不干净);②**P2 禁止隐式拉取**:部署/回滚/迁移的 `up` 带 `--pull never`,镜像缺失立即显式报错,不再静默从 registry 拉一个非归档版本(拉取类服务不受影响,步骤 5 已显式 pull;05 页栈启动刻意不加,其语义含拉取缺失镜像);③**S2 健康检查口径**:exited-0(一次性初始化服务的正常终态)按「完成」计入通过并逐条记日志,不再误报失败(此前与「部署失败自动回滚」叠加会把好部署回滚掉);④**S1 复核**:`import_compose` 失败残留目录的代码早已修复,wiki/07 过期记载更正。命令 114 不变;测试 501→505。本机 Docker 实测:孤儿 up/down 移除、`--pull never` 显式报错与拉取类回归。**第三十二批(回滚链审查修复)**:①回滚的 compose/override **一律「归档为准」**,06 页改显式 `-f` 链(实测默认文件解析会被 `compose.yaml` 遮蔽、且只吃一个 override);②`--no-build` 堵住「镜像缺失时现场构建非归档版本」;③预检新增 `noComposeCopy` 提示(两个入口均渲染);④up 后镜像不一致数写进回滚结果文案(历史/通知不撒谎);⑤**顺带修掉一个真 bug**:两页预检容器同 id,致 04 页结果写进 06 页容器、模态里空白;⑥wiki/07 补孤儿清理的项目作用域边界。测试 505→510。此前版本:v6.12.1(补丁:修复 up 后校验的 `docker inspect` 模板解析错误 —— 带点标签键误用字段链(`.Config.Labels."com.docker.compose.service"` 不是合法 Go 模板)致 docker CLI 以退出码 64 拒绝,校验每次部署皆失败(只告警不误判成败,但整条形同虚设);改用 `index` 读取、失败信息附远端输出尾部,并新增回归守护测试(形态断言 + 真机 CLI 解析)。测试 500→501。此前版本:v6.12.0(回滚/部署「按镜像 ID 收敛」批 —— 用户真机问题驱动:`goodlaser-backend:latest` 被外部指到别的 ID、归档镜像仍在服务器上(挂于其它标签),旧预检报「回不去」而用户无出路。①**up 前按 ID 收敛**(`plan_tag_convergence_pairs`,回滚/部署共用):标签没指向记录的镜像 ID 就 `docker tag` 零拷贝指回 —— 既治该真机案例,也把 TOCTOU 窗口(预检快照到 up 之间隔着逐包 load,外部主体改标签会使结论失效)压到最小;②预检四态化:新增 `tagRestore`(标签待指回,不阻断);③**up 后运行镜像校验**(`verify_running_images`:容器实际镜像 ≠ 期望即逐条告警,兜住 .env 漂移与未重建容器);④装载改**清单驱动**(目录里 manifest 未记录的 tar 跳过并告警 —— 治反向校验缺口);⑤**插值漂移预检**:归档 compose + 服务器当前 `.env` 重算并与 manifest 的 tag 比对,漂移须确认(判定基准复用 manifest 的 tag,零 schema 变更、旧归档可检)。命令 114 不变;测试 477→500。此前版本:v6.11.1((补丁:回滚中心(06 页)补上回滚预检 —— 第二十九批只做了 04 页入口,而 06 页是配置漂移(项目改名/被删)后的兜底路径,比 04 页更需要预检。`rollback_precheck` 与 `rollback_execute_stack_at` 均扩为双入口(项目 id / 项目目录)。第二十九批:回滚安全与体验八项——①**按 manifest 回滚 + 可用性预检**(`rollback_precheck` 新命令):逐服务核对镜像来源,智能传输跳过的服务按 manifest 记录的镜像 ID 在服务器上找回(不必强制留档),「回不去」的服务逐条列出并要求确认,不再静默跳过;②override 纳入归档与回滚(`.env` 刻意不入,见 wiki/07);③06 页回滚可取消(此前 04 页取消按钮与托盘停止在该路径都不可用);④失败的回滚也记入历史(带原因,可复盘);⑤设置新增开机自启(零依赖注册表实现,状态以系统实况为准);⑥部署页执行按钮常驻底部 + 配置/执行分区;⑦回滚确认区压缩(不再挤下方日志框);⑧新增 `verify/user-facing-copy.js` 守护用户可见文案(此前帮助文案漂移无人拦截)。命令 113→114;测试 461→474。第二十八批:候选池余项四项全部完成——①B4 允许执行时段(日程加 windowStart/windowEnd,半开区间、支持跨 0 点;到点不在时段内则**当天跳过且不算错过**);②B2 部署日报(按天聚合 DeployRecord 成一条摘要经通知管道发出;设置项 digestHour 缺省关 + 通知中心「部署日报」订阅 + 独立状态文件防当天重发);③B1 多项目编排(批量模态内维度切换:项目多选 → 各自默认服务器;队列去重/续传/重跑改用 serverId|projectId 复合键);④C1 传输中可取消(SshClient 挂传输级取消探针,SFTP 上传/下载与 docker save 流按 64KB 块检查;19 处既有调用点零签名改动)。命令 113 不变;测试 444→461。第二十七批:A 组清尾四项 + B3 服务器标签——①回滚中心容器计数改按 compose working_dir 标签精确归属(同前缀项目不再互相串数);②清理分析归档名放宽(自定义命名归档可列出,排序统一退回 mtime,与部署侧裁剪、回滚明细、迁移取最近 N 个同源);③项目迁移 tar 镜像可配置(设置项 tarImage,服务器有私有 registry/白名单时用自备镜像);④归档搬运注释与实现对齐(仅注释);⑤B3 服务器标签:ServerConfig.tags + 03 页按首标签分节 + 部署/回滚中心/定时/项目迁移四处下拉 optgroup + 部署页批量勾选列表分节 + 服务器表单标签编辑器(chips + 下拉 + 新建)。命令 113 不变;测试 432→444。第二十六批:池内后五项——①network/internal 错误码挂点(update.rs 全链路挂码 + 前端按码分流);②扫描识别放宽(compose 文件名与深度可配,拼远端命令前严格校验);③模板驱动批量(批量模态内选项级套用模板);④卷内容浏览/单卷备份(逐层 tar 列表 + tar.gz 下载到本机);⑤迁移断点续传(阶段级,复用同一张断点表 + mode 隔离)。命令 111→113;测试 413→432。**功能候选池(16 项)至此全部完成**。第二十六批:第二十五批:五项功能批——①SSH config/known_hosts 导入(只读扫描 + 勾选批量建服务器,不预填指纹);②架构预检(镜像 vs 服务器架构,只告警不阻断,防 exec format error);③本地镜像清理(02 页悬空镜像扫描/删除,逐 ID rmi + 两步确认);④栈 compose 查看/编辑(保存前自动备份 .ddbak 保留 3 份);⑤部署失败自动回滚(整栈健康检查失败时回到上一份归档,默认关)。命令 106→111;测试 377→413。第二十四批:第二十四批:双会话并行第二/三批——①资源阈值告警(S1,连续 2 轮防抖 + alert 通知类型 + 设置中心四字段);②多机巡检汇总(S2,纯前端串行复用 server_diagnose + 汇总视图);③05 页各 Tab 搜索筛选 + 容器批量操作(S1,pause/unpause/rename + 常显勾选列 + 批量条 + 改名模态)。测试 370→377;命令 106 不变。第二十三批:v6.5.0 双会话首对——第二十三批:双会话并行开发首对——①S1 细节补正 7 项(终端输入框/.env 占位符对比度 AA、终端模态 gap/字号统一、help 文案与实现对齐、wiki/07 四处、servers.js 注释、manage.rs 死常量、import_compose 失败残留清理);②S2 终端日志保留(时间制)(AppSettings.termLogKeepDays 默认 30 天 0=永久,开终端 + 启动时按文件名时间戳 best-effort 清理 logs/term-*.log,设置中心「通用」区新增数字输入)。命令 106 不变;测试 363→370。第二十二批补丁:终端多标签可达性修复——模态内新增「＋新标签」下拉,修复「只能开一个标签」;第二十二批:第三梯队三项 + 文档治理——①定时/延迟部署(deploy_schedule.rs 新模块:每天 HH:MM / 一次性,后端 30s tick 自主发起,错过不补跑;前置「后端远程操作互斥收口」REMOTE_OP_IN_FLIGHT + RAII guard);②终端多标签 + 同栈广播 + 会话输出自动落盘 logs/term-*.log;③配置版本历史(config/.history/ 写前自动快照 + config_history_list/restore 两命令);④文档治理(清扫 + 七篇页首版本戳 + verify/contract-smoke.js + 编排层补测试)。命令 101→106;测试 335→360。补丁3 v6.3.3:项目迁移默认命名兜底;补丁2 v6.3.2:版本对比按镜像 ID;补丁1 v6.3.1:诊断凭据修复。第二十一批:v6.3.0 第二梯队五项;第二十批:v6.2.0 第一梯队五项;第十九批补丁3:v6.1.3 RSA 私钥修复 + 四路全量审查;第十八批:v6.0.0 安全强化与并发互斥。)
```

## 权威计划/完成记录(仓库根目录)

- `UPGRADE-PLAN.md` — 三批升级计划与完成记录,是 v4.6.0 后全部功能的事实来源:**第一批** v4.7-v5.1 五阶段(智能传输/一键回滚 → 通知中心 → 连接与数据安全 → 桌面体验 → 远程管理补遗,已一次性并入 v5.1.0 发布);**第二批** 阶段六至十(断点续传 ✅ / 批量部署 ✅ / 清理分析 ✅ / 实时日志 ✅ / 镜像迁移 ✅),阶段六起以 **v5.2.0** 发版,阶段九/十随 **v5.3.0** 收官;**第三批** 阶段十一至十四(清理分析重构与分项目清理 ✅ / 项目源更新 ✅ / 独立回滚中心 ✅ / 映射默认名 ✅),随 **v5.4.0** 发布(修复并入 **v5.4.1 / v5.4.2**:自动更新自动重启与完成提示、项目源绑定入口、检查汇总栏、从源更新的改动判定);第四批:项目级部署目录与项目默认服务器、部署页选择记忆与联动、部署历史补 id(随 **v5.5.0** 发布);第五批:项目级归档保留数量 + 回滚中心逐版本删除(随 **v5.6.0** 发布);**第六批:项目跨服务器迁移(镜像/compose/数据卷/归档整体搬运 + 改绑)、项目表单的远程部署目录检测与创建、镜像迁移两处失效修复(随 v5.7.0 发布),ark 设计细节优化 pass 3(按钮/输入框/动效 + 样式技术治理,随 v5.7.1 发布);文字样式统一(字号归并 10 档阶梯 / 确认框三段式 confirmBlock / 微标签两轨 / 断词分类,随 v5.7.2 发布);**设计细节 pass 4(ark × ui-ux-pro-max 评估:模态焦点三件套 + toast/进度 SR 播报 + 焦点环对比修复 + 动效/阴影/z-index token 收敛 + 按压态补齐 + 批量徽章口径修正 + 监控阈值 tick 与 Tab 面板 wipe,随 v5.7.3 发布);字体轨道筛查统一(mono/cond 仅 ASCII + 栈内 CJK 兜底 + 17 类中文主体元素出轨道,根除中文回退宋体,随 v5.7.4 发布);**第七批:回滚中心明细加载批量化(恒单次往返,根除 N+1 卡顿)+ 归档版本说明(类 GitHub Release 的标题/描述,存归档目录 release-notes.json,版本详情模态查看/编辑,随 v5.8.0 发布)**;**第八批:检查更新后自动弹出更新确认模态(圆润卡片,展示更新内容——主路径 notes 由按 tag 抓取的 Release body 填充;一次确认全自动下载安装重启,随 v5.8.1 发布)**;**第九批(分阶段路线图阶段一):设置中心「打开日志文件夹」(open_logs_dir)+ errText 统一上收 app.js + 文档计数同步,随 v5.8.2 发布**;**第十批(阶段二):远程管理概览新增宿主机磁盘用量(df -kP 采样,根分区 + Docker 数据盘独立挂载时单列),随 v5.9.0 发布**;**第十一批(阶段三):部署时预填版本标题/说明(整栈可选输入,成功后自动补写归档 release-notes.json;部署历史落盘调整为先于 deploy-done;真机反馈修复版本详情保存路径翻倍——第七批潜伏 bug),随 v5.10.0 发布**;**第十二批(阶段四):代码结构治理——commands.rs 按功能域拆为 src/commands/ 11 文件(lib.rs 零改动)+ manage_logs/manage_stats 补测,JS 大文件拆分(deploy/manage/servers 三大文件拆出 4 个模态/独立域文件),随 v5.11.0 发布**;**第十三批(阶段五):表单校验体系——失焦校验 + Enter 提交 + form 语义(五处模态接入;顺带修复拆分的 DeployKit 覆盖 bug),随 v5.12.0 发布**;**第十四批(阶段六):批量部署增强——单台/一键批量续传 + 停止批量即时中止当前台 + 断点裁剪回收临时 tar,并更正「批量不落断点」的错误文档(顺带修复批量第二台卡死的 deferred 形态 bug)
- `DOCKER-MANAGE-PLAN.md` — 远程管理模块三阶段实施计划与 A/B/C 完成记录(v4.5.0,历史事实来源)

## 历史过程文档(已清理)

v1 设计 spec、实现计划、UI 设计简报等过程产物原存放于 `docs/`,已在 v4.6.0 收尾(commit 3d6f9f3)整体删除;"为什么这么做"的记录由本 wiki 的设计决策记录(见 07)与 `UPGRADE-PLAN.md` / `DOCKER-MANAGE-PLAN.md` 的完成记录承接。冲突时以代码与本 wiki 为准。
