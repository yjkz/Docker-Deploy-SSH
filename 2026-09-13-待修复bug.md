# 2026-09-13 全量审查 — 待修复 Bug 清单与修复提示词

> 来源:2026-09-13 五路并行审查(wiki 01-07 + Rust + JS + 契约)+ 主代理复核。
> 基线:v6.1.2(commit ffdcd17),`cargo test` 320 passed / 13 ignored,命令 95。
> 用法:每个 Bug 附「修复提示词」,可直接粘贴给执行代理/新会话逐条修复;
> 排序 = 建议处理顺序(P0 → P1 → P2)。所有行号基于 ffdcd17,动手前先 `git log --oneline -1` 校验。
> 完成一条就更新本文档状态列;全部修完后并入 ROADMAP「已完成」并同步 wiki/07。

## 进度看板(持续更新)

> 注:P0~P2-3 与 P2-6①(后端挂码)已随另一会话的 **v6.1.3(5917753)** 推送;
> P2-4/P2-5/P2-6②/P2-7 在 **v6.1.4(第二十批修复批收尾)** 落地。

| 项 | 状态 | 修复摘要 |
|---|---|---|
| P0 批量续传脱离互斥 | ✅ 已修复(v6.1.3 推送) | resumeKey 分支置位 `st.deploying`/`ddRemoteOp` + `refreshControls()`/`renderProgress(0)`,invoke 失败分支同步还原两把锁;复位由 handleDone 批量分支自然承接 |
| P1-1 探活间隔不回显 | ✅ 已修复(v6.1.3 推送) | settings.js 回填去 `=== ''` 恒假守卫改无条件回填;代理字段(预填 '')保留守卫,已注释说明 |
| P1-2 Esc 连带关模态 | ✅ 已修复(v6.1.3 推送) | app.js 新增全局仲裁 `window.isTopModal(id)`(DOM 序最后 = 视觉最上层,与 Tab 圈禁同款);全站 **11 处** Esc 监听全部接入(servers 内嵌 cleanup 逐层、deploy 3 模态、settings+update-confirm、help、config-io、notify、manage、rollback);verify 三脚本 PASS |
| P1-3 导入 SMTP 二次加密 | ✅ 已根治(v6.2.0 第二十批「配置导入预览」) | `config_import_preview` 不落盘试运行返回摘要(含 `smtp_password_present`);前端两段式——预览「SMTP 跨机需重录」明示 + confirmBlock 确认后才真导入;v6.1.3 已先行 notify 脱敏成对+导出预检 |
| P2-1 建连超时缺失 | ✅ 已修复(v6.1.3 推送) | 单镜像 probe + 步骤3 补 `with_timeout(15s)`;顺带补 host_server.rs 安装 Docker 建连;全仓 30 处 connect 审计仅剩测试内 5 处裸调用(合规) |
| P2-2 日志流泄漏+stop 竞态 | ✅ 已修复(v6.1.3 推送) | start 锁段内「信号旧代号句柄→begin→注册新句柄」(被顶替旧流立即退出,不再等 3600s);stop 锁内「快照代号+取句柄+推进状态」锁外发信号(停旧开新竞态消除);`end()` 并入锁段删除;+3 行为单测全绿 |
| P2-3 DD_CONFIG_DIR 生产生效 | ✅ 已修复(v6.1.3 推送) | `app_dir()` 环境变量分支套 `#[cfg(debug_assertions)]`(编译期裁剪,release 剔除);测试 32 处使用点不受影响(dev profile),CI 也是 dev profile;release build 编译通过 |
| P2-4 save_config 族互斥 | ✅ 已修复(v6.1.4) | config.rs 新增 `update_config<T,F>`(CONFIG_LOCK 锁内 load→闭包改→save,Err 不落盘)+ RESUME_LOCK;**11 个生产写点全部迁移**:save_server_entry / save_config_cmd(restore_sentinels_and_save 并入锁段,消除「还原读到现值→落盘前被改」窗口)/ notify_save_config / remember_key_passphrase / persist_host_key_if_needed / retrust_host_key / bind+update_project_from_source / import_compose push / bind_project_to_target / save+remove_checkpoint;DPAPI 加密、文件复制均锁外;并发压力测试 8 线程×5 条零丢写 + Err 闭包不落盘 |
| P2-5 encrypt_password 哨兵 | ✅ 已修复(v6.1.4) | 入口拒收 `"*"`(挂 input 码,文案指向「重输真实密码或留空沿用」);+单测(拒绝+挂码+正常明文对照) |
| P2-6 错误码两处口径 | ✅ 已修复(①随 v6.1.3 推送;②v6.1.4) | ① exec_forwarded_inner 超时挂 `[dderr:timeout]`;② app.js 新增 `window.errCodeOf(payload)`(errorCode 字段优先,回退 parseErrCode message,落地 wiki/04:644「字段优先」契约),deploy.js 三处消费点(单发收尾/批量续传 deferred/批量部署 deferred)全部接线 |
| P2-7 杂项(版本号/AppBus) | ✅ 已修复(v6.1.4) | package.json version 1.0.0→6.1.4 对齐应用版本(发版链不消费它,纯对齐);rollback.js 两处裸 `__TAURI__.event.listen` 改 `AppBus.on`(全站唯一例外消除,防御口径与其它 7 模块一致;listen 失败复位守卫保留) |

---

## P0 — 批量续传台脱离全部互斥与取消机制

| 项 | 值 |
|---|---|
| 状态 | ✅ 已修复(2026-09-14,修复记录见文末「修复实施记录」) |
| 位置 | `ui/deploy.js:1964-1986`(`runBatchNext` 的 `item.resumeKey` 分支) |
| 对照 | 正确先例:`ui/deploy.js:416-417`(单发续传 `onResumeStart` 两者都设) |

**问题**:批量「续传此台/一键续传」走 `deploy_resume_start` 时,既没设 `st.deploying = true` 也没持 `window.ddRemoteOp = 'deploy'`。后果:①「停止批量」(`onStopBatch`,deploy.js:2126 判 `st.deploying`)对续传台**不发 `cancel_deploy`**,与 wiki/06:87「停止批量=当前台步骤边界即时中止」的承诺在续传场景失效;②「取消部署」按钮(`onCancelDeploy`,deploy.js:1698 判 `!st.deploying` return)在批量续传期间无效;③ 06 页回滚不被 `ddRemoteOp` 拦阻,可与续传并发操作同一台服务器的 releases 目录(回滚 load/tag/up 与续传 load/up 交错)——第十八批引入的部署/回滚跨页互斥在此被架空。

**修复提示词**:

```
修复 DockerDeploy SSH(D:\Github-repositories\docker-deploy-ssh)中批量续传台脱离互斥与取消机制的 bug。

背景:ui/deploy.js 的 runBatchNext()(约 1964-1986 行)在 item.resumeKey 分支里直接
AppBus.invoke('deploy_resume_start'),既没有 st.deploying = true 也没有
window.ddRemoteOp = 'deploy'。对照单发续传 onResumeStart(约 416-417 行),两者都设。
后果:「停止批量」(onStopBatch,约 2126 行,判 st.deploying 才发 cancel_deploy)对正在
续传的当前台失效;「取消部署」(onCancelDeploy,约 1698 行,判 !st.deploying 即 return)
无效;06 页回滚不被 window.ddRemoteOp 拦阻,可与批量续传并发操作同一服务器的
releases 目录。

要求:
1. 在 resumeKey 分支发起 deploy_resume_start 之前,与 startDeploy 同款置位:
   st.deploying = true; window.ddRemoteOp = 'deploy'(跨页互斥锁,06 页回滚据此避让)。
2. 确认复位路径已对接:handleDone 的批量分支(约 1716-1721 行)本就会在单台收尾时
   复位 st.deploying 与 ddRemoteOp——若批量续传台也走该入口则无需另写复位;若不走,
   补复位。注意不要把整批队列提前解锁(批量循环 runBatchNext 自身有 st.batch.active
   守卫,确认两把锁不互相干扰)。
3. refreshControls() 需要在置位后调用,让按钮态立即切换(参照 onResumeStart 的做法)。
4. 验证:node --check ui/deploy.js;跑 verify/ 三脚本;浏览器桩(_tauri-stub)模拟批量
   一台失败→「续传未完成服务器」→ 断言续传期间 window.ddRemoteOp === 'deploy' 且
   「停止批量」按钮可点并发出 cancel_deploy。按项目验证工作流(ROADMAP.md 第 30 行)
   做浏览器桩验证,不要用 computer-use。
5. 硬约束:不动 st.rb* 回滚体系;不动后端;改完把本文档该条状态改为 ✅。
```

---

## P1-1 — 设置页「探活间隔」永不回显,重存 0 会静默关掉探活

| 项 | 值 |
|---|---|
| 状态 | ✅ 已修复(2026-09-14,修复记录见文末「修复实施记录」) |
| 位置 | `ui/settings.js:636-637`(回填守卫)+ `ui/settings.js:248-250`(预填 '0') |

**问题**:「服务器探活间隔」input 构建时预填默认值 `'0'`,而回填守卫写的是 `if (probe && probe.value === '')`——`probe.value` 恒为 `'0'` 非空,守卫恒假,**用户保存的间隔(如 5)重开设置模态时永不回显**。用户误以为没保存,再存一次 0,与 `probe.rs:34-56`「按设置启停」联动后即**静默关掉探活**。代理字段不受影响(其预填 `''`,settings.js:269)。

**修复提示词**:

```
修复 DockerDeploy SSH(D:\Github-repositories\docker-deploy-ssh)设置页探活间隔不回显的 bug。

背景:ui/settings.js 约 248-250 行 buildField 构建探活间隔 input 时预填了默认值 '0';
而回填处(约 636-637 行)写的是 if (probe && probe.value === '') probe.value = String(s.probeIntervalMins || 0);
由于 value 恒为 '0' 非空,守卫恒假,保存值(如 5)永不回显。用户重开设置看到 0、误以为
未保存、再存 0,经后端 probe.rs sync_from_settings 即静默关闭探活任务。

要求:
1. 去掉 probe.value === '' 守卫,改为无条件回填:if (probe) probe.value = String(s.probeIntervalMins || 0);
   与同文件 checkbox 的回填方式一致。
2. 顺带检查同函数内其它 input 的回填守卫是否同型(proxy 字段预填 '' 所以无恙,确认即可)。
3. 验证:node --check ui/settings.js;浏览器桩打开设置模态两次:第一次设 5 保存→重开
   断言 input.value === '5';再确认保存 0 → 重开显示 '0'。verify 三脚本跑一遍。
4. 改完把本文档该条状态改为 ✅。
```

---

## P1-2 — Esc 一键关闭两层模态,编辑中的服务器表单被连带丢弃

| 项 | 值 |
|---|---|
| 状态 | ✅ 已修复(2026-09-14,修复记录见文末「修复实施记录」) |
| 位置 | `ui/servers.js:2565-2572`(document 级 Escape 监听) |
| 对照 | 正确先例:`ui/settings.js:648-654`(检测上层模态可见则转发,一次只关一层) |

**问题**:servers.js 的 document 级 keydown 在 Escape 时**先无条件 `closeModal()`**(关闭 servers-modal,里面可能有未保存的服务器/项目表单),再关叠在上面的 cleanup-modal。清理分析模态叠在服务器编辑表单上时,用户按一次 Esc 想关清理面板,结果编辑中的表单也被关闭,**输入内容丢失**。系统性根因:Esc 无「最上层模态」仲裁(Tab 圈禁在 app.js:392-396 有全局仲裁,Esc 是 8 个模块各自注册 document keydown)。

**修复提示词**:

```
修复 DockerDeploy SSH(D:\Github-repositories\docker-deploy-ssh)中 Esc 连带关闭两层模态的 bug。

背景:ui/servers.js 约 2565-2572 行,document 级 keydown 监听 Escape 时先无条件 closeModal()
(关 servers-modal,可能含未保存的编辑表单),再关叠在上面的 cleanup-modal——一次 Esc
两层全关,表单内容丢失。正确先例在同仓 ui/settings.js 约 648-654 行:closeSettingsModal
先检测上层 update-confirm-modal 是否可见并转发给上层守卫,一次 Esc 只关一层。

要求:
1. 最小修法(推荐,零架构改动):照抄 settings.js 的转发模式——servers.js 的 Esc 处理里,
   先判断 cleanup-modal(以及其它可能叠在 servers-modal 上的模态,全文件搜 class 含
   modal-overlay 的 id 逐一核对)是否可见,可见则只关最上层、return;不可见才关
   servers-modal。
2. 若发现叠层不止一处(如 help-modal 也可能叠上),统一按「DOM 序最后的开启 modal =
   视觉最上层」仲裁(app.js 392-396 行 Tab 圈禁已有同款函数,可复用/上收),各模块 Esc
   改为只关自己且仅当自己是顶层。改动面大就退回最小修法,在改动处注释说明取舍。
3. 验证:node --check;浏览器桩场景:打开服务器编辑表单输入内容→点「清理分析」叠上
   cleanup-modal→按 Esc→断言只有 cleanup-modal 关闭、servers-modal 仍在且输入未丢;
   再按一次 Esc 才关表单。help-modal 叠加场景同样验证一次。
4. 改完把本文档该条状态改为 ✅。
```

---

## P1-3 — 跨机器导入配置时 SMTP 密码静默损坏(二次加密)

| 项 | 值 |
|---|---|
| 状态 | ✅ 已根治(v6.2.0 第二十批「配置导入预览」:`config_import_preview` 不落盘试运行 + 跨机 SMTP 重录明示 + confirmBlock 二次确认;下述提示词为历史存档) |
| 位置 | `src-tauri/src/config_io.rs:269-291`(`import_notify`)+ `:344-356`(导入写盘,注释自认绕过 NOTIFY_UNHEALTHY) |

**问题**:导出文件 blob 里 SMTP `password_enc` 是**旧机器上解出的明文**;导入时 `import_notify` 把它当明文 `dpapi_protect` 再加密——这本身对「导出→导入」是正确设计。真正的缺陷在**跨机场景语义缺失**:导入是「整体替换」且绕过 NOTIFY_UNHEALTHY,导入成功后 `notify.json` 合法、密码却在新机器永远解不开前的旧密文路径无任何提示——用户只有发信失败才察觉。配套缺口:导入前无预览(选错文件即静默丢当前全部配置)。

**修复提示词**:

```
为 DockerDeploy SSH(D:\Github-repositories\docker-deploy-ssh)补配置导入预览,根治跨机
SMTP 密码静默损坏问题。

背景:src-tauri/src/config_io.rs 的 config_import_file(约 328-360 行)是「解密→整体
替换写盘」,无预览、无确认摘要;import_notify(约 269-291 行)把 blob 内明文 SMTP 密码
DPAPI 重加密(这对导出→导入链路本身正确)。问题:跨机器导入后 SMTP 密码字段合法但
在新机无意义,用户发信失败才察觉;且选错备份文件会静默覆盖当前全部配置。

要求:
1. 新增 Tauri 命令 config_import_preview(path: String, password: String) ->
   ImportPreview(camelCase 序列化),放 config_io.rs。实现:复用 open_blob 解密 +
   ExportPayload 反序列化(抽出与 config_import_file 共用的「读文件→解密→解析」
   纯函数,如 parse_export_file(path, password) -> Result<ExportPayload, String>,
   两条命令都调它,DRY),然后**不落盘**地计算摘要:
   - servers_total / projects_total(备份内数量)
   - current_servers / current_projects(当前配置数量,load_config)
   - smtp_present: 备份内 SMTP 密码非空 → true(前端提示「导入后需在新机重录 SMTP 密码」
     ——DPAPI 密文不可跨机,这是明示而非静默)
   - 无新增依赖;命令注册进 lib.rs generate_handler!(95→96)并在 wiki/04 登记。
2. config_import_file 改为复用同一 parse_export_file,行为零变化。
3. 前端 ui/config-io.js runImport(约 145-200 行)改两段式:先 config_import_preview
   → confirmBlock 弹摘要(「备份含 N 台服务器/M 个项目,将整体覆盖当前 X 台/Y 个;
   SMTP 密码导入后需重新输入」)→ 用户确认才调 config_import_file。复用现有
   window.confirmBlock({title, facts, risk}) 三段式,不用 window.confirm。
4. 单测:parse_export_file 往返(构造 ExportEnvelope → 解析回 ExportPayload 字段一致);
   ImportPreview camelCase 序列化形态;config_import_preview 不落盘(调用前后
   servers.json mtime/内容不变——用 DD_CONFIG_DIR 注入临时目录,仓库已有该先例)。
5. 验证:cargo test 显式看 test result: ok(基线 320,不许把失败吞进管道);clippy 新
   代码零警告;node --check;浏览器桩跑「选文件→预览摘要→确认→导入」全流程。
6. 硬约束:新命令必在 lib.rs 注册 + wiki/04 登记(计数同步);导入仍是整体替换语义
   不改;改完把本文档该条状态改为 ✅。
```

---

## P2-1 — 单镜像管线两处 SSH 建连无 15s 超时

| 项 | 值 |
|---|---|
| 状态 | ✅ 已修复(2026-09-14,修复记录见文末「修复实施记录」) |
| 位置 | `src-tauri/src/commands/deploy.rs:246`(智能传输判定 probe 连接)、`deploy.rs:347-349`(步骤 3 上传建连) |
| 对照 | 整栈管线 `deploy.rs:1423-1429`、manage `manage.rs:75-81` 均有 `with_timeout(SSH_CONNECT_TIMEOUT_SECS,…)`;常量在 `commands/mod.rs:137` |

**问题**:wiki/02:34 与 mod.rs:136 注释均声称 `SSH_CONNECT_TIMEOUT_SECS=15`「统一兜底」,但单镜像管线两处建连裸 `SshClient::connect(...).await?`。不可达主机/丢包网络下单镜像部署与续传可挂起至 OS TCP 超时(分钟级),期间取消标志无从生效。

**修复提示词**:

```
修复 DockerDeploy SSH(D:\Github-repositories\docker-deploy-ssh)单镜像管线 SSH 建连缺
15s 超时的问题。

背景:src-tauri/src/commands/deploy.rs 约 246 行(智能传输判定 probe 连接)与约
347-349 行(步骤 3 上传建连)是裸 SshClient::connect(...).await?,没有套 with_timeout;
整栈管线(约 1423-1429 行)与 manage.rs(约 75-81 行)都套了
with_timeout(SSH_CONNECT_TIMEOUT_SECS, "连接超时", "请检查服务器地址与网络", …)。
不可达主机时单镜像部署可挂到 OS TCP 超时(分钟级),期间取消标志无效。

要求:
1. 两处建连补套 with_timeout(SSH_CONNECT_TIMEOUT_SECS, "连接超时",
   "请检查服务器地址与网络", …),与整栈管线同款(SSH_CONNECT_TIMEOUT_SECS 在
   commands/mod.rs 约 137 行,同 crate 可见性已满足)。
2. 注意 deploy.rs:337 附近单镜像有 probe.take() 复用连接的设计——若 probe 连接成功
   后被复用到步骤 3,步骤 3 处只需在「重新建连」分支补超时,不要对复用路径多余包裹。
3. 顺带:全仓 grep SshClient::connect,逐处确认是否都有超时兜底(除测试),发现其它
   裸调用一并列出(只修本条两处,其余报告不改)。
4. 验证:cargo test 显式 test result: ok(基线 320);clippy 零新增;该路径无单测基础
   的话不强加(连接超时是集成行为),改动小、以代码审查 + grep 清单为准。
5. 改完把本文档该条状态改为 ✅。
```

---

## P2-2 — 被顶替的日志流永不退出(SSH 连接 + 远端 docker logs -f 泄漏)

| 项 | 值 |
|---|---|
| 状态 | ✅ 已修复(2026-09-14,修复记录见文末「修复实施记录」) |
| 位置 | `src-tauri/src/manage_logs.rs:14-16`(注释承诺自清理)、`:246-275`(实际无周期代号检查)、`:298-309`(stop 的 generation 竞态窗口) |

**问题**:模块头注释称重复 start 时「旧任务发现代号过期即退出并 emit eof」,实际 `is_current(gen)` 只在输出行回调里检查(`manage_logs.rs:247`),`run_log_stream` 主体无周期检查——被顶替的旧流不退出、不 emit eof,继续持有 SSH 连接与远端 `docker logs -f` 直到自然结束或 3600s 兜底。前端靠「先 stop 再 start」(`manage-stacks.js:1265`)规避,但 stop/start 是两个独立 invoke,存在毫秒级竞态(stop 读 generation 时已是新值→误停**新**流、旧流漏网)。

**修复提示词**:

```
修复 DockerDeploy SSH(D:\Github-repositories\docker-deploy-ssh)manage_logs 被顶替日志流
不退出与 stop 竞态问题。

背景:src-tauri/src/manage_logs.rs 模块头注释(约 14-16 行)承诺「旧任务发现代号过期
即退出并 emit eof」,但 is_current(gen) 只在输出行回调(约 247 行)里检查,run_log_stream
主体无周期代号检查——被顶替的旧流持有 SSH 连接与远端 docker logs -f 直到 3600s 兜底。
且 stop(约 298-309 行)与 start 是两个独立 invoke:快速「停旧开新」时 stop 读到的
generation 可能已是新值,误停新流、旧流漏网。

要求:
1. 旧流自清理:start 新流时(或 begin() 内),遍历 streams 表把**旧代号**的 cancel
   sender 逐一 send(()),让旧任务立即走既有取消路径(关通道返回);这同时修复
   「注释承诺 vs 实现不符」——实现落地后注释保持原样即可成立。
2. stop 竞态:stop 按调用方给的 streamId/generation 精确停(先查表再发信号),不要
   「读当前 generation 再停」;若现有结构做不到精确,退而求其次:stop 时只停「当前
   之前」的代号,并在 start 与 stop 间加 Mutex 串行化(同进程命令,锁竞争可忽略)。
3. 保持既有语义:单行 16KB 截断、3600s 兜底、事件 payload(manage-logs {streamId,
   data, eof})零变化;被顶替旧流退出时不给新流发 eof(避免前端误判流结束)——
   若现有 on_line 静默机制已保证,确认并注释说明。
4. 单测:manage_logs 现有 2 个测试,补「顶替旧流会收到取消信号」的行为测试(不连真
   SSH,用现有测试基建的 mock/句柄断言;若现有基建做不到端到端,至少覆盖新增的
   「按代号精确停」纯函数部分)。
5. 验证:cargo test 显式 test result: ok;clippy 零新增;node --check(若前端零改动
   则免);浏览器桩快速开关日志跟随几次,断言无「新流被误停」(跟随开关正常工作)。
6. 改完把本文档该条状态改为 ✅。
```

---

## P2-3 — `DD_CONFIG_DIR` 环境变量在生产构建同样生效

| 项 | 值 |
|---|---|
| 状态 | ✅ 已修复(2026-09-14,修复记录见文末「修复实施记录」) |
| 位置 | `src-tauri/src/config.rs:308-317`(`app_dir()`) |

**问题**:测试注入机制污染了生产代码路径——`DD_CONFIG_DIR` 无 `debug_assertions` 守卫,release 构建中任何启动脚本/外部程序/快捷方式注入该变量即可重定向配置根(可伪造 `host_key_sha256` 跳过 TOFU 告警;更现实的危害是用户「配置神秘丢失」的困惑)。

**修复提示词**:

```
修复 DockerDeploy SSH(D:\Github-repositories\docker-deploy-ssh)DD_CONFIG_DIR 在生产构建
同样生效的安全问题。

背景:src-tauri/src/config.rs 约 308-317 行 app_dir() 无条件读 DD_CONFIG_DIR 环境变量
重定向配置根,这是测试注入机制,却在 release 构建同样生效。外部注入可把 config/ 指到
攻击者可控目录(能读到伪造的 servers.json,含伪造 host_key_sha256 跳过 TOFU 告警;
DPAPI 密文不可伪造)。更现实的风险是用户环境残留该变量导致「配置丢失」假象。

要求:
1. 用 #[cfg(debug_assertions)] 守卫环境变量分支:debug 构建保留注入能力(测试依赖它,
   全仓 grep DD_CONFIG_DIR 先列出全部使用点——src/commands/tests.rs 等处测试确实依赖),
   release 构建恒走 exe 同目录。
2. 注意:#[cfg] 是编译期裁剪,测试代码(cfg(test))编译时 debug_assertions 为真,现有
   测试不受影响——跑全量 cargo test 验证。
3. 若有测试在 release profile 下跑(检查 CI/脚本,cargo test --release 若在用),确认
   该场景不依赖注入;在用则改用显式 API(如 config::set_test_dir)替代环境变量,取舍
   记录在改动注释。
4. 顺带:app_dir() 内 current_exe().expect(...) 是 wiki/02:8 明文豁免的 unwrap,本次
   不动(另案);不要顺手扩大改动面。
5. 验证:cargo test 显式 test result: ok(基线 320);cargo build --release 通过;
   debug 构建 DD_CONFIG_DIR 注入路径的行为测试仍绿。
6. 改完把本文档该条状态改为 ✅。
```

---

## P2-4 — `save_config` 族无跨调用互斥(并发丢配置)

| 项 | 值 |
|---|---|
| 状态 | ✅ 已修复(v6.1.4,修复记录见文末「修复实施记录」)
| 位置 | `save_server_entry`(commands/mod.rs:334)/ `save_config_cmd`(:306)/ `notify_save_config`(notify.rs:224)/ `save_checkpoint`(config.rs:627) |

**问题**:四个写路径都是 `load_config() → 改 → save_config()` 的 read-modify-write,无跨调用互斥。前端几乎同时触发两个保存(如服务器编辑 + 通知中心保存)时,后写覆盖先写,**合法地丢配置**——与 v6.1.1 哨兵事故同为「合法写坏数据」形态,NOTIFY_UNHEALTHY 救不了(写回的是合法但过期的数据)。`save_checkpoint` 同型(批量 + 单发并发部署时丢断点)。

**修复提示词**:

```
修复 DockerDeploy SSH(D:\Github-repositories\docker-deploy-ssh)save_config 族并发
read-modify-write 互踩导致丢配置的问题。

背景:save_server_entry(src-tauri/src/commands/mod.rs 约 334)、save_config_cmd(约 306)、
notify_save_config(src-tauri/src/notify.rs 约 224)、save_checkpoint(src-tauri/src/config.rs
约 627)都是 load → 改 → 全量写回,无互斥。并发触发时后写覆盖先写,静默丢失先到的修改
(合法但过期的数据,UNHEALTHY 机制不覆盖)。save_checkpoint 同型:并发部署丢断点。

要求:
1. config.rs 增加进程级常驻锁:static CONFIG_RW_LOCK: Mutex<()>(OnceLock 包裹初始化,
   参考仓库既有 TEST_DIR_LOCK 先例)。提供两个包装:
   - save_config_locked(整量保存:锁内 load→调用方已改好的 cfg→save)
   - update_config<F: FnOnce(&mut AppConfig) -> Result<()>>(读改写收口:锁内 load→改→save)
   注意:锁只保「读-改-写」原子性,不保「调用方在锁外读了旧快照再整量写」——所以
   **改造方向是把四个写点全部改走 update_config 闭包式**(改的动作移进闭包),而不是
   只在 save_config 加锁(那只锁了写半程,没用)。
2. 四个调用点逐个迁移:
   - save_server_entry:update_config(|cfg| { 找条目→merge 密文/指纹→替换/插入 })
   - save_config_cmd:保留「前端整量写回」语义,但锁内先 load 磁盘现值做哨兵还原
     (现有 restore_sentinel 逻辑原样移进锁内)
   - notify_save_config:同理,notify 字段改写移进闭包
   - save_checkpoint:锁内 load_resume_map→insert→trim→write
3. 死锁防御:闭包内不得再调用任何会取同一把锁的函数(update_config 不可嵌套;闭包内
   只做内存操作 + 最后一次 save)。在 update_config 文档注释写明。
4. 单测:并发写压力测试(spawn 10 个线程各自 update_config 追加一个 server,断言最终
   10 个全在);现有哨兵/merge 测试迁移后保持绿。
5. 验证:cargo test 显式 test result: ok(基线 320 + 新增);clippy 零新增。
6. 硬约束:这是行为面敏感改动,改动后跑全量测试 + verify 三脚本;改完把本文档该条
   状态改为 ✅。
```

---

## P2-5 — 哨兵纵深防御缺口:`encrypt_password` 不拒收 `"*"`

| 项 | 值 |
|---|---|
| 状态 | ✅ 已修复(v6.1.4,修复记录见文末「修复实施记录」)
| 位置 | `src-tauri/src/commands/mod.rs:373-375`(`encrypt_password`);哨兵机制 `:288-297` |

**问题**:若任何前端路径把只读视图哨兵 `"*"` 当「新密码明文」传给 `encrypt_password`,会得到 `DPAPI("*")`——它不等于 `"*"`,后端 `restore_sentinel` 不还原,**真实密文照样被覆盖**。当前前端没有这条路径(已复核 servers.js:1947-1982),但 v6.1.1 事故证明这类防线不能只靠调用方自觉。

**修复提示词**:

```
修复 DockerDeploy SSH(D:\Github-repositories\docker-deploy-ssh)encrypt_password 不拒收
哨兵值的纵深防御缺口。

背景:src-tauri/src/commands/mod.rs 约 373-375 行 encrypt_password 直接把任意明文 DPAPI
加密返回。get_config 只读视图的密文哨兵是 "*"(CIPHER_SENTINEL,约 288 行)。若前端把
"*" 当新密码明文传入,得到 DPAPI("*") 密文——它不等于 "*" 本身,save_server_entry 的
restore_sentinel(约 292-297 行)不会还原,真实密文被覆盖,后续 SSH 认证用 "*" 当密码
必然失败。当前前端无此调用路径(预防性纵深防御,v6.1.1 同类事故的教训)。

要求:
1. encrypt_password 入口拒收哨兵:if plain == CIPHER_SENTINEL { return Err(错误文案
   说明「不能把占位符当密码保存,请输入真实密码」) }。错误串走 errors::tagged(
   ErrCode::Input, …) 挂码。
2. 单测:encrypt_password("*") 返回 Err 且码为 input;正常明文不受影响(该函数依赖
   DPAPI,测试断言 Ok 分支即可,仓库 crypto 测试已有 DPAPI 同机先例)。
3. 验证:cargo test 显式 test result: ok;clippy 零新增。
4. 改完把本文档该条状态改为 ✅。
```

---

## P2-6 — 错误码两处口径不一致(挂码缺失 + 契约字段未消费)

| 项 | 值 |
|---|---|
| 状态 | ✅ 已修复(v6.1.4,修复记录见文末「修复实施记录」)
| 位置 | ① `src-tauri/src/commands/mod.rs:773-774`(exec 超时未挂 timeout 码);② `ui/deploy.js:1737/1974/2010`(deploy-done.errorCode 字段从未读取,wiki/04:644 契约「字段优先」落空) |

**问题**:① `exec_forwarded_inner` 超时分支 `format!("远端命令执行超时({} 秒): {}")` 未走 `errors::tagged(ErrCode::Timeout,…)`,与第十六批「判定一律按码」方向不一致(前端/通知无法按码归类);② 前端只 `parseErrCode(p.message)`,后端 mod.rs:548 恰好保留了原始标记所以能工作,但契约规定「优先读字段,缺失回退 parse message」——若后端未来剥码,取消判定会静默退化为中文文案匹配。

**修复提示词**:

```
修复 DockerDeploy SSH(D:\Github-repositories\docker-deploy-ssh)错误码两处口径不一致。

背景与要求:
① src-tauri/src/commands/mod.rs 约 773-774 行 exec_forwarded_inner 超时分支返回
   format!("远端命令执行超时({} 秒): {}", …) 未挂错误码。改为
   crate::errors::tagged(crate::errors::ErrCode::Timeout, 原文案)——文案不变只加
   \u001f[dderr:timeout] 前缀。对照 manage.rs 约 54-56 行 with_timeout 的挂码先例。
② ui/deploy.js 三处(约 1737、1974、2010 行)读 deploy-done payload 只做
   parseErrCode(p)(解析 p.message 内嵌标记),从未读 p.errorCode 字段。按 wiki/04:644
   契约「优先读字段(errorCode),缺失时回退对 message 做 parseErrCode」补齐:
   var code = (p && p.errorCode) ? p.errorCode : parseErrCode(p);
   后续判定统一用 code。三处一致改,别漏批量分支。
验证:
- cargo test 显式 test result: ok(基线 320;①如有既有断言文案原文的测试需同步);
  clippy 零新增;node --check ui/deploy.js;verify 三脚本。
- 浏览器桩:模拟 deploy-done 带 errorCode='canceled' 且 message 不含标记 → 断言前端
  判定为取消;再模拟两处都无 → 回退路径仍工作。
改完把本文档该条状态改为 ✅。
```

---

## P2-7 — 杂项小项(可合并为一次提交)

| 项 | 值 |
|---|---|
| 状态 | ✅ 已修复(v6.1.4,修复记录见文末「修复实施记录」)
| 位置 | ① `package.json:3` version="1.0.0" 与应用版本脱钩;② `ui/rollback.js:82/85` 绕过 AppBus 直用 `__TAURI__.event.listen`(违反 wiki/03:25 封装纪律,防御口径与其它 7 模块不一致) |

**修复提示词**:

```
修复 DockerDeploy SSH(D:\Github-repositories\docker-deploy-ssh)两个杂项小问题,一次提交。

① package.json 第 3 行 version "1.0.0" 与应用版本(6.1.2)脱钩。发版链不消费它,但
  既有就该对齐:改成与 src-tauri/tauri.conf.json 一致的版本号;并在 ROADMAP「三处
  版本号」验证清单备注加第四处(或确认 release.yml 防呆是否需要扩——目前只比对
  tag vs tauri.conf.json,不需要,备注即可)。
② ui/rollback.js 约 82/85 行直接使用 window.__TAURI__.event.listen,违反「前端一律经
  AppBus 调用」纪律(其它 7 个模块都走 AppBus.on,其带 __TAURI__ 缺失防护)。改为
  AppBus.on('事件名', handler)(核对 AppBus.on 的签名与返回值,若 rollback.js 用了
  listen 返回的 unlisten 函数,确认 AppBus.on 是否返回同等句柄;不返回则 AppBus 补
  返回,保持零行为差异)。改动后该模块的 listen 失败守卫(logBound 复位)逻辑保持。
验证:node --check ui/rollback.js(+ app.js 若改);verify 三脚本(bridge/scope 会扫
  桥接完整性);浏览器桩进 06 页确认监听仍生效(事件到达有 UI 反应)。
改完把本文档该条状态改为 ✅。
```

---

## 附:审查发现的「非 bug 但建议跟进」项(已有归属,不在本清单重复展开)

- **文档不符清扫 + verify/doc-consistency.js + wiki 页首版本戳**:见 ROADMAP.md 候选池「文档/治理批」表(本次审查 90% 文档问题同根:版本戳靠人工同步)。
- **commands/ 编排层 0 in-file 测试、ssh.rs 测试密度**:见候选池「文档/治理批」末行。
- **新功能第一梯队(第二十批)**:部署历史筛选 / 配置导入预览(=P1-3 的根治方案)/ 服务器一键诊断 / 部署模板 / 通知耗时阈值——见 ROADMAP.md 候选池「第一梯队」表。
- **第二、三梯队**:批量报告导出 / 回滚两版本对比 / 启动静默检查更新 / 更新失败回执 / 托盘闭环 / 定时部署 / 终端多标签 / 配置版本历史——见 ROADMAP.md 候选池对应表。

> 处理顺序建议:P0 → P1-1 → P1-2 → P1-3(或并入第二十批阶段二)→ P2-1~P2-7 → 文档/治理批。
> 每条修完跑验证链(ROADMAP.md 执行协议第 3 条);涉及命令增删的记得 wiki/04 计数同步。

---

# 修复实施记录(持续更新)

## 2026-09-14:第一轮(P0 / P1-1 / P1-2 / P2-1 / P2-2 / P2-3)

### P0 批量续传脱离互斥 ✅

`ui/deploy.js` `runBatchNext` 的 `item.resumeKey` 分支:
- 发起 `deploy_resume_start` 前置位 `st.deploying = true` + `window.ddRemoteOp = 'deploy'`,并调 `refreshControls()` / `renderProgress(0, '')`(与单发续传 `onResumeStart` 同款);
- invoke 级失败分支(断点已被清理/参数异常)同步还原两把锁再 resolve deferred,防互斥锁悬挂;
- 复位由 `handleDone` 批量分支(`st.batch.deferred` 非空即走)自然承接,零额外复位代码。
- 验证:`node --check` 通过;待浏览器桩断言「续传期间 ddRemoteOp === 'deploy' 且停止批量可发 cancel_deploy」(并入本批收尾验证)。

### P1-1 探活间隔不回显 ✅

`ui/settings.js` 设置回填:探活间隔 input 的 `probe.value === ''` 恒假守卫(构建时预填 `'0'`)去除,改无条件 `probe.value = String(s.probeIntervalMins || 0)`;代理字段(预填 `''`)保留守卫并注释差异原因。

### P1-2 Esc 连带关模态 ✅

- `ui/app.js` 新增全局仲裁 `window.isTopModal(modalId)`:`querySelectorAll('.modal-overlay:not(.hidden)')` 取 DOM 序最后一个(= 视觉最上层)与目标比对 —— 与 Tab 圈禁(app.js 原有)同一仲裁逻辑;
- 全站 **11 处** Esc 监听全部改判 `isTopModal`(servers 的 cleanup→servers 逐层链、deploy 的 deploy-modal/deploy-batch-modal/migrate-project-modal 三模态、settings-modal + update-confirm-modal、help、config-io、notify、manage、rollback 的 release-detail);
- settings 的 `closeSettingsModal` 内原有「转发给上层 update-confirm」逻辑保留作双保险。
- 验证:`node --check` 9 个改动文件全过;verify 三脚本(54 断言/bridge/scope)全 PASS。

### P2-1 建连超时缺失 ✅

- `commands/deploy.rs` 单镜像管线两处(智能传输 probe `:251` 附近、步骤 3 上传建连 `:359` 附近)补 `with_timeout(SSH_CONNECT_TIMEOUT_SECS, "连接超时", "请检查服务器地址与网络", …)`,与整栈管线同款;skip_transfer 分支复用 probe 连接不经建连,注释说明不包裹理由;
- 顺带补 `commands/host_server.rs` `install_server_docker` 的裸建连(原全仓审计发现的第 3 处);
- 全仓审计结论:30 处 `SshClient::connect` 调用点全部有超时兜底,仅 `ssh.rs` 测试模块内 5 处裸调用(测试自带超时,合规)。

### P2-2 日志流泄漏 + stop 竞态 ✅

`src-tauri/src/manage_logs.rs`:
- **start 锁段重构**:「给旧代号句柄发取消信号 → `begin()` → 注册新句柄」在同一 `streams` 锁临界区完成 —— 被顶替的旧流立即经 `exec_streaming` 取消路径关通道返回,不再持有 SSH 连接与远端 `docker logs -f` 等到 3600s 兜底(模块头注释同步改写,承诺落地);
- **stop 锁段重构**:「快照代号 + 取句柄 + 推进状态(generation+1、running=false)」在同一锁内完成,锁外 `send(())` —— 消除「停旧开新」竞态(stop 读到新代号误停新流);原三步无锁的 `end()` 方法删除并入锁段;
- 本地 sender `drop` 保证取消只经表内句柄发生,语义单一;
- 新增 3 个行为单测(旧流恰收到一次信号/新流不被误伤/句柄表清理防误删),`test_begin_end_monotonic` 去 `end()` 依赖;manage_logs 4 测试全绿,clippy 对本文件零警告(顺带清掉 `for_kv_map` 2 处)。

### P2-3 DD_CONFIG_DIR 生产生效 ✅

`src-tauri/src/config.rs` `app_dir()`:环境变量分支套 `#[cfg(debug_assertions)]`(编译期裁剪,release 构建整条分支被剔除);`config_dir()` 文档注释同步标注 debug 专属。测试 32 处 `DD_CONFIG_DIR` 使用点不受影响(`cargo test` 默认 dev profile,`cfg(test)` 与 `debug_assertions` 均为真);CI(.github/workflows/ci.yml `cargo test`)同为 dev profile。
- 验证补跑(2026-09-14):`cargo test --lib` **324 passed**(当时基线)+ `cargo build --release` 编译通过。

---

## 2026-09-14:第二轮 v6.1.4(P2-4 / P2-5 / P2-6② / P2-7)

> 背景:P0~P2-3 与 P2-6①(后端挂码)已随另一会话的 v6.1.3(5917753,RSA 私钥修复
> + 密文链路 P1-P4 + 体验审查 8 处)一并推送;本会话在其上接力完成剩余四项,
> 版本顺延 v6.1.4。两轮改动经逐 hunk 对比**零冲突**(5917753 提交的就是本会话
> 第一轮的 ui 侧文件;Rust 侧 notify.rs 虽双方都动,但改的是不同函数)。

### P2-4 save_config 族互斥 ✅(本批最大改动)

`src-tauri/src/config.rs` 新增:
- `static CONFIG_LOCK` + `pub fn update_config<T, F>(mutate) -> Result<T, String>`:
  锁内 load → 闭包改 → save;**闭包返回 Err 时不落盘**(整体放弃);文档注释写明
  「不可嵌套/锁内只做内存修改」约束;
- `static RESUME_LOCK`:断点表 RMW 独立锁(与主配置三件互不相干,合用会让部署写
  断点阻塞配置保存)。

**11 个生产写点全部迁移**(`load → 改 → save` 三步整体进锁):
save_server_entry(merge 逻辑进闭包)/ save_config_cmd(重构为 `restore_sentinels_and_save`,
哨兵还原的「读现值」与「落盘」同锁,消除「还原读到现值→落盘前被并发保存改写」窗口;
原「磁盘不可读+含哨兵拒绝保存」语义由 update_config 的 load 失败前置拦截继承)/
notify_save_config(DPAPI 加密锁外;notify 字段替换进闭包,servers/projects 落闭包内
最新磁盘值,不再被本函数旧快照冲掉)/ remember_key_passphrase / persist_host_key_if_needed
(TOFU 落盘)/ retrust_host_key / import_compose(push)/ bind_project_source /
update_project_from_source(.bak 备份与 copy_compose_bundle 在锁外,配置合并进闭包)/
bind_project_to_target(migrate)/ save_checkpoint + remove_checkpoint(RESUME_LOCK)。

测试 +2:`test_update_config_concurrent_no_lost_write`(8 线程 × 5 条并发追加,断言
40 条全在)、`test_update_config_error_aborts_write`(Err 闭包不落盘);哨兵/merge
既有测试迁移后全绿。

### P2-5 encrypt_password 拒收哨兵 ✅

`commands/mod.rs`:入口 `plain == CIPHER_SENTINEL` → Err(`errors::tagged(Input, …)`,
文案「不能把占位符当密码保存…请输入真实密码或留空以沿用已存密码」)。+单测
`test_encrypt_password_rejects_sentinel`(拒绝 + 挂 input 码 + 正常明文对照)。

### P2-6② 前端消费 errorCode 字段 ✅(①已随 v6.1.3)

`ui/app.js` 新增 `window.errCodeOf(payload)`:payload.errorCode 字段(非空字符串)
优先,回退 `parseErrCode(message)` —— 落地 wiki/04:644「字段优先」契约。`ui/deploy.js`
三处 deploy-done 消费点接线:handleDone 取消判定 / 批量续传 deferred / 批量部署
deferred(原三处只 parse message)。

### P2-7 杂项 ✅

- `package.json` version `1.0.0` → `6.1.4`(与 tauri.conf.json/Cargo.toml 对齐;发版链
  不消费它,纯一致性——「三处版本号」之外的第 4 处,备注见 ROADMAP 该批记录);
- `ui/rollback.js` 两处裸 `window.__TAURI__.event.listen` → `AppBus.on`(全站唯一绕过
  AppBus 的例外消除;listen 失败复位 logBound 守卫原样保留;try/catch 同步拦截不再
  需要——AppBus.on 内部有 `__TAURI__` 缺失防护)。

### 验证(v6.1.4 全量)

- `cargo test`:**328 passed / 0 failed / 13 ignored**(v6.1.3 基线 325 + 本批 3:
  并发零丢写 / Err 不落盘 / 哨兵拒绝)——显式确认 test result 行
- `cargo clippy --all-targets` 零新增警告
- `node --check` ui/ 全部 16 个 JS 通过;verify 三脚本(scope/bridge/form 54 断言)全 PASS
